import { describe, it } from 'vitest'

describe('App routing', () => {
  it.todo('renders Routes with /input, /, /topology, /rack paths')
  it.todo('NavLink for / uses end prop')
  it.todo('NavLink strip has aria-label="page navigation"')
  it.todo('FC mode hides rack NavLink')
})
