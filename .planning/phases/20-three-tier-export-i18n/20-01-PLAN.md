---
phase: 20-three-tier-export-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/export/exportThreeTierCsv.ts
  - src/features/export/exportThreeTierCsv.test.ts
  - src/features/export/exportThreeTierPdf.ts
  - src/features/export/exportThreeTierPdf.test.ts
  - src/features/export/pdf/ThreeTierNetStackDocument.tsx
  - src/features/export/pdf/ThreeTierBOMPage.tsx
  - src/features/export/pdf/ThreeTierInputsPage.tsx
  - src/features/export/exportConvergedCsv.ts
  - src/features/export/exportConvergedPdf.ts
  - src/features/export/pdf/ConvergedNetStackDocument.tsx
  - src/features/export/index.ts
  - src/components/TopBar.tsx
autonomous: true
requirements: [TEXP-01, TEXP-02]

must_haves:
  truths:
    - "Clicking CSV in three-tier standalone mode downloads a CSV file with access/aggregation/core sections"
    - "Clicking PDF in three-tier standalone mode downloads a PDF with three-tier BOM tables and topology page"
    - "Clicking CSV in converged mode with topology=three-tier exports three-tier rows instead of Ethernet rows"
    - "Clicking PDF in converged mode with topology=three-tier includes three-tier BOM pages instead of Ethernet pages"
  artifacts:
    - path: "src/features/export/exportThreeTierCsv.ts"
      provides: "buildThreeTierCsvRows, buildThreeTierCsvString, downloadThreeTierBomCsv"
      exports: ["buildThreeTierCsvRows", "buildThreeTierCsvString", "downloadThreeTierBomCsv"]
    - path: "src/features/export/exportThreeTierPdf.ts"
      provides: "generateThreeTierPdfBlob"
      exports: ["generateThreeTierPdfBlob"]
    - path: "src/features/export/pdf/ThreeTierNetStackDocument.tsx"
      provides: "ThreeTierNetStackDocument PDF document component"
      exports: ["ThreeTierNetStackDocument"]
    - path: "src/features/export/pdf/ThreeTierBOMPage.tsx"
      provides: "Three-tier BOM page for PDF with access/aggr/core sections"
      exports: ["ThreeTierBOMPage"]
    - path: "src/components/TopBar.tsx"
      provides: "mode==='three-tier' branch in handleCsv and handlePdf"
  key_links:
    - from: "src/components/TopBar.tsx"
      to: "src/features/export/exportThreeTierCsv.ts"
      via: "import downloadThreeTierBomCsv"
      pattern: "downloadThreeTierBomCsv"
    - from: "src/components/TopBar.tsx"
      to: "src/features/export/exportThreeTierPdf.ts"
      via: "import generateThreeTierPdfBlob"
      pattern: "generateThreeTierPdfBlob"
    - from: "src/components/TopBar.tsx"
      to: "src/store/threeTierResultStore.ts"
      via: "import useThreeTierResultStore"
      pattern: "useThreeTierResultStore"
    - from: "src/features/export/exportConvergedCsv.ts"
      to: "src/features/export/exportThreeTierCsv.ts"
      via: "import buildThreeTierCsvRows for topology=three-tier"
      pattern: "buildThreeTierCsvRows"
    - from: "src/features/export/pdf/ConvergedNetStackDocument.tsx"
      to: "src/features/export/pdf/ThreeTierBOMPage.tsx"
      via: "conditionally renders ThreeTierBOMPage when threeTierBom is present"
      pattern: "ThreeTierBOMPage"
---

<objective>
Three-tier CSV and PDF export for standalone and converged modes.

Purpose: Users need to export their three-tier network designs to CSV (for spreadsheet analysis) and PDF (for stakeholder reports). This covers standalone three-tier mode and the three-tier topology variant within converged mode.

Output: 7 new files (CSV export, PDF export, PDF document component, BOM page, inputs page, and their tests), plus updates to converged exports, barrel, and TopBar.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Prior phase summaries needed for export pattern reference -->
@.planning/phases/17-converged-export-i18n/17-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/domain/schemas/three-tier-bom.ts:
```typescript
export type ThreeTierBOM = z.infer<typeof ThreeTierBOMSchema>
// Fields: racks, networkRacks, accessSwitches, aggregationSwitches, coreSwitches,
//   oobSwitches, borderLeafSwitches, serverAccessCables, accessAggrCables, aggrCoreCables,
//   serverOobCables, vltCables, sfp28Count, qsfp28Count, qsfp56ddCount,
//   accessToAggrOversubscription, aggrToCoreOversubscription,
//   switchPositioning, recommendedCableLengthM, violations, input
```

From src/domain/schemas/three-tier-input.ts:
```typescript
export type ThreeTierSizingInput = z.infer<typeof ThreeTierSizingInputSchema>
// Fields: racks, portsPerServerFrontend, portsPerServerBackend, connectivityType, cableType,
//   accessModel, activeUplinksPerAccess, aggregationModel, activeUplinksPerAggregation,
//   coreModel, borderLeafModel, borderLeafCount, rackSize, serverUHeight, switchPositioning
```

From src/domain/schemas/converged-bom.ts:
```typescript
export type ConvergedBOM = z.infer<typeof ConvergedBOMSchema>
// Fields: topology ('leaf-spine'|'three-tier'), ethernetBom (nullable), threeTierBom (nullable),
//   fcBom (nullable), violations, input
```

From src/store/threeTierResultStore.ts:
```typescript
export const useThreeTierResultStore = create<ThreeTierResultState>()
// State: { bom: ThreeTierBOM | null, violations: ThreeTierConstraintViolation[] }
```

From src/features/export/exportCsv.ts:
```typescript
export function wrapCsvValue(value: string): string
export function buildCsvRows(bom: NetworkBOM): string[]
```

From src/features/export/exportConvergedCsv.ts:
```typescript
export function buildConvergedCsvString(bom: ConvergedBOM): string
// Already guards: bom.ethernetBom ? buildCsvRows(bom.ethernetBom) : []
```

From src/features/topology/index.ts:
```typescript
export { getLastTopologyPng } from './TopologyCanvas'
export { getLastFCTopologyPng } from './fc/FCTopologyCanvas'
// NOTE: No getLastThreeTierTopologyPng exists yet. ThreeTierTopologyTab does NOT
// have PNG capture. For PDF topology page, pass undefined (same pattern as existing).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three-tier standalone CSV export with TDD</name>
  <files>
    src/features/export/exportThreeTierCsv.ts,
    src/features/export/exportThreeTierCsv.test.ts
  </files>
  <read_first>
    src/features/export/exportCsv.ts,
    src/features/export/exportCsv.test.ts,
    src/domain/schemas/three-tier-bom.ts,
    src/domain/schemas/three-tier-input.ts,
    src/features/export/exportConvergedCsv.test.ts
  </read_first>
  <behavior>
    - buildThreeTierCsvRows(bom) returns string[] with access/aggregation/core switch rows
    - buildThreeTierCsvString(bom) starts with UTF-8 BOM character (0xFEFF)
    - buildThreeTierCsvString(bom) has exactly one header row (Category,Model / Type,Role,Quantity,Unit,Connectivity,Notes)
    - CSV contains rows for access switches (role "Access"), aggregation switches (role "Aggregation"), core switches (role "Core"), OOB switches (role "OOB")
    - CSV contains cable rows: Server-Access, Access-Aggr, Aggr-Core, Server-OOB, VLT Interconnect
    - CSV contains transceiver rows when sfp28Count > 0 or qsfp28Count > 0 or qsfp56ddCount > 0
    - CSV contains border leaf row when borderLeafSwitches > 0
    - CSV contains violation note rows for AGGREGATION_CAPACITY_EXCEEDED, CORE_CAPACITY_EXCEEDED, OOB_PORT_SATURATION, DAC_DISTANCE_ADVISORY, RACK_CAPACITY_EXCEEDED
    - downloadThreeTierBomCsv(bom) triggers download with filename "netstack-three-tier-bom.csv"
  </behavior>
  <action>
    RED phase: Create exportThreeTierCsv.test.ts with a mock ThreeTierBOM fixture (2 racks, 4 access switches, 2 aggregation, 2 core, 1 OOB, cables). Write tests for all behaviors above. Run `rtk vitest run src/features/export/exportThreeTierCsv.test.ts` -- all must FAIL.

    GREEN phase: Create exportThreeTierCsv.ts following the exact pattern of exportCsv.ts:
    - Import `wrapCsvValue` from './exportCsv' (already exported)
    - Import `ThreeTierBOM` from '@/domain/schemas/three-tier-bom'
    - `buildThreeTierCsvRows(bom: ThreeTierBOM): string[]` -- builds 7-column rows:
      - Switch rows: [Switch, bom.input.accessModel, "Access", bom.accessSwitches, "each", conn, ""]
      - Switch rows: [Switch, bom.input.aggregationModel, "Aggregation", bom.aggregationSwitches, "each", conn, ""]
      - Switch rows: [Switch, bom.input.coreModel, "Core", bom.coreSwitches, "each", conn, ""]
      - Switch rows: [Switch, "S3248T-ON", "OOB", bom.oobSwitches, "each", "1G", ""]
      - Border leaf row (only if bom.borderLeafSwitches > 0): [Switch, bom.input.borderLeafModel, "Border Leaf", bom.borderLeafSwitches, "each", conn, ""]
      - Cable rows: Server-Access, Access-Aggr, Aggr-Core, Server-OOB, VLT Interconnect (same column pattern as exportCsv.ts)
      - Transceiver rows for sfp28Count, qsfp28Count, qsfp56ddCount (QSFP56-DD for 400G Z-series links)
      - Violation note rows following same pattern as exportCsv.ts
    - `buildThreeTierCsvString(bom)` -- BOM_CHAR + CRLF + header + rows (identical structure to buildCsvString)
    - `downloadThreeTierBomCsv(bom)` -- download trigger with filename 'netstack-three-tier-bom.csv'
    - conn variable from bom.input.connectivityType, cableLabel from bom.input.cableType

    Run `rtk vitest run src/features/export/exportThreeTierCsv.test.ts` -- all must PASS.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/network-sizer && rtk vitest run src/features/export/exportThreeTierCsv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "buildThreeTierCsvRows" src/features/export/exportThreeTierCsv.ts
    - grep -q "buildThreeTierCsvString" src/features/export/exportThreeTierCsv.ts
    - grep -q "downloadThreeTierBomCsv" src/features/export/exportThreeTierCsv.ts
    - grep -q "Access" src/features/export/exportThreeTierCsv.ts
    - grep -q "Aggregation" src/features/export/exportThreeTierCsv.ts
    - grep -q "Core" src/features/export/exportThreeTierCsv.ts
    - grep -q "netstack-three-tier-bom.csv" src/features/export/exportThreeTierCsv.ts
    - test file exists: src/features/export/exportThreeTierCsv.test.ts
  </acceptance_criteria>
  <done>buildThreeTierCsvRows produces correct 7-column rows for access/aggr/core/OOB switches, inter-tier cables, transceivers, and violation notes. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three-tier standalone PDF export, converged export updates, TopBar wiring, and barrel</name>
  <files>
    src/features/export/pdf/ThreeTierBOMPage.tsx,
    src/features/export/pdf/ThreeTierInputsPage.tsx,
    src/features/export/pdf/ThreeTierNetStackDocument.tsx,
    src/features/export/exportThreeTierPdf.ts,
    src/features/export/exportThreeTierPdf.test.ts,
    src/features/export/exportConvergedCsv.ts,
    src/features/export/exportConvergedPdf.ts,
    src/features/export/pdf/ConvergedNetStackDocument.tsx,
    src/features/export/index.ts,
    src/components/TopBar.tsx
  </files>
  <read_first>
    src/features/export/pdf/BOMPage.tsx,
    src/features/export/pdf/InputsPage.tsx,
    src/features/export/pdf/NetStackDocument.tsx,
    src/features/export/pdf/ConvergedNetStackDocument.tsx,
    src/features/export/exportPdf.ts,
    src/features/export/exportConvergedPdf.ts,
    src/features/export/exportConvergedPdf.test.ts,
    src/features/export/exportConvergedCsv.ts,
    src/features/export/index.ts,
    src/components/TopBar.tsx,
    src/store/threeTierResultStore.ts,
    src/features/topology/index.ts
  </read_first>
  <behavior>
    - generateThreeTierPdfBlob(bom) returns a Blob (mocked @react-pdf/renderer)
    - generateThreeTierPdfBlob(bom, topoDiagramPng) accepts optional topology PNG
    - ThreeTierNetStackDocument renders CoverPage + ThreeTierInputsPage + ThreeTierBOMPage + TopologyPage + ViolationsPage
    - ThreeTierBOMPage displays access/aggregation/core switch tables with dual oversubscription ratios
    - ConvergedNetStackDocument conditionally renders ThreeTierBOMPage/ThreeTierInputsPage when bom.threeTierBom is present (instead of Ethernet pages)
    - buildConvergedCsvString handles topology='three-tier' by using buildThreeTierCsvRows instead of buildCsvRows
    - TopBar handleCsv has mode==='three-tier' branch calling downloadThreeTierBomCsv
    - TopBar handlePdf has mode==='three-tier' branch calling generateThreeTierPdfBlob
  </behavior>
  <action>
    **A) ThreeTierBOMPage.tsx** -- New PDF page for three-tier BOM, following BOMPage.tsx pattern:
    - Import `{ Page, View, Text, StyleSheet }` from '@react-pdf/renderer'
    - Import `ThreeTierBOM` from '@/domain/schemas/three-tier-bom'
    - Props: `{ bom: ThreeTierBOM }`
    - Dual oversubscription ratios: access-to-aggr and aggr-to-core, each with its own severity block using thresholds green<=3, amber 3-5, red >5 (per Phase 19-01 decision)
    - Switch table with rows: [accessModel, "Access", accessSwitches], [aggregationModel, "Aggregation", aggregationSwitches], [coreModel, "Core", coreSwitches], [S3248T-ON, "OOB", oobSwitches], plus optional border leaf row
    - Cable table with rows: Server-Access, Access-Aggr, Aggr-Core, Server-OOB, VLT Interconnect, plus transceiver rows (SFP28, QSFP28, QSFP56-DD)
    - Use exact same StyleSheet pattern as BOMPage.tsx (copy styles)

    **B) ThreeTierInputsPage.tsx** -- New PDF page for three-tier inputs, following InputsPage.tsx pattern:
    - Import `ThreeTierSizingInput` from '@/domain/schemas/three-tier-input'
    - Props: `{ input: ThreeTierSizingInput }`
    - Display: rack count, servers per rack, access/aggregation/core models, uplinks per tier, connectivity, cable type, switch positioning
    - Same StyleSheet as InputsPage.tsx

    **C) ThreeTierNetStackDocument.tsx** -- Document component following NetStackDocument.tsx pattern:
    - Import `Document` from '@react-pdf/renderer'
    - Import `ThreeTierBOM` from '@/domain/schemas/three-tier-bom'
    - Import CoverPage, ThreeTierInputsPage, ThreeTierBOMPage, TopologyPage, ViolationsPage (reuse existing TopologyPage and ViolationsPage -- ViolationsPage accepts any violations array since it renders code+message pairs)
    - Props: `{ bom: ThreeTierBOM, topoDiagramPng?: string }`
    - NOTE: ViolationsPage expects Ethernet ConstraintViolation[]. For three-tier violations, create a simple ThreeTierViolationsPage that maps ThreeTierConstraintViolation codes to human-readable messages. OR, if ViolationsPage is flexible enough, reuse it. Check ViolationsPage props first. If it expects `violations: ConstraintViolation[]` specifically, create a new simple violations renderer inline in the document that maps violation codes to text.
    - Document title: "NetStack -- Three-Tier Sizing Report"

    **D) exportThreeTierPdf.ts** -- Following exportPdf.ts pattern exactly:
    - `generateThreeTierPdfBlob(bom: ThreeTierBOM, topoDiagramPng?: string): Promise<Blob>`
    - Dynamic import of '@react-pdf/renderer' and './pdf/ThreeTierNetStackDocument'
    - Same double-cast pattern through unknown for pdf() call

    **E) exportThreeTierPdf.test.ts** -- Following exportConvergedPdf.test.ts pattern:
    - Mock @react-pdf/renderer
    - Test: generateThreeTierPdfBlob returns a Blob
    - Test: generateThreeTierPdfBlob with topoDiagramPng returns a Blob

    **F) Update exportConvergedCsv.ts** -- In `buildConvergedCsvString`:
    - Import `buildThreeTierCsvRows` from './exportThreeTierCsv'
    - When `bom.threeTierBom` is non-null (topology=three-tier), use `buildThreeTierCsvRows(bom.threeTierBom)` instead of `buildCsvRows(bom.ethernetBom!)` for the network rows
    - Add "Section,Three-Tier" separator when both threeTierBom and fcBom are present (mirroring "Section,Ethernet" pattern)

    **G) Update ConvergedNetStackDocument.tsx** -- Conditional three-tier pages:
    - Import ThreeTierBOMPage, ThreeTierInputsPage
    - When `bom.threeTierBom` is non-null, render ThreeTierInputsPage + ThreeTierBOMPage + TopologyPage (instead of the Ethernet InputsPage + BOMPage + TopologyPage block)
    - Keep the existing ethernetBom conditional block, just add a parallel threeTierBom block

    **H) Update exportConvergedPdf.ts** -- No changes needed if ConvergedNetStackDocument handles the conditional rendering internally. Verify props are passed through.

    **I) Update index.ts barrel** -- Add exports:
    - `export { downloadThreeTierBomCsv, buildThreeTierCsvString, buildThreeTierCsvRows } from './exportThreeTierCsv'`
    - `export { generateThreeTierPdfBlob } from './exportThreeTierPdf'`

    **J) Update TopBar.tsx**:
    - Import `useThreeTierResultStore` from '@/store/threeTierResultStore'
    - Import `downloadThreeTierBomCsv` from '@/features/export/exportThreeTierCsv'
    - Import `generateThreeTierPdfBlob` from '@/features/export/exportThreeTierPdf'
    - Add `const threeTierBom = useThreeTierResultStore(useShallow((s) => s.bom))`
    - Update `activeBom`: add `mode === 'three-tier' ? threeTierBom :` before existing chain
    - In `handleCsv`: add `if (mode === 'three-tier' && threeTierBom) downloadThreeTierBomCsv(threeTierBom)` as first branch (before converged)
    - In `handlePdf`: add `if (mode === 'three-tier')` block following converged pattern -- get topology PNG (pass undefined since no capture exists yet), generate blob, trigger download with filename 'netstack-three-tier-report.pdf'

    Run all tests: `rtk vitest run` -- all 514+ tests must pass. Run `rtk tsc --noEmit` -- no type errors.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/network-sizer && rtk vitest run && rtk tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ThreeTierBOMPage" src/features/export/pdf/ThreeTierBOMPage.tsx
    - grep -q "ThreeTierInputsPage" src/features/export/pdf/ThreeTierInputsPage.tsx
    - grep -q "ThreeTierNetStackDocument" src/features/export/pdf/ThreeTierNetStackDocument.tsx
    - grep -q "generateThreeTierPdfBlob" src/features/export/exportThreeTierPdf.ts
    - grep -q "three-tier" src/components/TopBar.tsx
    - grep -q "useThreeTierResultStore" src/components/TopBar.tsx
    - grep -q "downloadThreeTierBomCsv" src/components/TopBar.tsx
    - grep -q "generateThreeTierPdfBlob" src/components/TopBar.tsx
    - grep -q "buildThreeTierCsvRows" src/features/export/exportConvergedCsv.ts
    - grep -q "threeTierBom" src/features/export/pdf/ConvergedNetStackDocument.tsx
    - grep -q "ThreeTierBOMPage" src/features/export/pdf/ConvergedNetStackDocument.tsx
    - grep -q "downloadThreeTierBomCsv" src/features/export/index.ts
    - grep -q "generateThreeTierPdfBlob" src/features/export/index.ts
    - test file exists: src/features/export/exportThreeTierPdf.test.ts
  </acceptance_criteria>
  <done>Three-tier PDF export generates correct document. Converged CSV/PDF conditionally use three-tier BOM when topology=three-tier. TopBar dispatches to three-tier exports. All tests pass, TSC clean.</done>
</task>

</tasks>

<verification>
1. `rtk vitest run` -- all tests pass (514+ baseline + new tests)
2. `rtk tsc --noEmit` -- no TypeScript errors
3. In standalone three-tier mode, CSV/PDF buttons produce downloads with access/aggregation/core sections
4. In converged mode with topology=three-tier, CSV/PDF exports include three-tier BOM data
</verification>

<success_criteria>
- Three-tier standalone mode CSV export produces 7-column rows grouped by access/aggregation/core tiers
- Three-tier standalone mode PDF export produces a multi-page document with cover, inputs, BOM, topology, and violations pages
- Converged mode with topology=three-tier exports three-tier sections in both CSV and PDF
- TopBar correctly dispatches to three-tier export functions when mode==='three-tier'
- All existing tests continue to pass (zero regressions)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-three-tier-export-i18n/20-01-SUMMARY.md`
</output>
