---
phase: 20-three-tier-export-i18n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/it/translation.json
autonomous: true
requirements: [TEXP-03]

must_haves:
  truths:
    - "Three-tier export button labels appear in English"
    - "Three-tier export button labels appear in French"
    - "Three-tier export button labels appear in German"
    - "Three-tier export button labels appear in Italian"
    - "All three-tier i18n keys are present and consistent across all 4 locales"
  artifacts:
    - path: "src/i18n/locales/en/translation.json"
      provides: "English three-tier export keys"
      contains: "threeTierCsvButton"
    - path: "src/i18n/locales/fr/translation.json"
      provides: "French three-tier export keys"
      contains: "threeTierCsvButton"
    - path: "src/i18n/locales/de/translation.json"
      provides: "German three-tier export keys"
      contains: "threeTierCsvButton"
    - path: "src/i18n/locales/it/translation.json"
      provides: "Italian three-tier export keys"
      contains: "threeTierCsvButton"
  key_links:
    - from: "src/i18n/locales/*/translation.json"
      to: "src/components/TopBar.tsx"
      via: "t('export.threeTierCsvButton') and t('export.threeTierPdfButton')"
      pattern: "threeTierCsvButton"
---

<objective>
Add three-tier export i18n keys to all 4 locale files and audit for completeness.

Purpose: All three-tier mode labels must appear correctly in EN, FR, DE, and IT. This includes export button labels and any missing three-tier BOM/mode labels.

Output: Updated 4 locale JSON files with complete three-tier export keys.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Prior i18n pattern reference -->
@.planning/phases/17-converged-export-i18n/17-02-SUMMARY.md

<interfaces>
<!-- Existing i18n key structure the executor needs to follow -->

From src/i18n/locales/en/translation.json (export section):
```json
{
  "export": {
    "csvButton": "Export CSV",
    "pdfButton": "Export PDF",
    "fcCsvButton": "Export FC CSV",
    "fcPdfButton": "Export FC PDF",
    "convergedCsvButton": "Export Converged CSV",
    "convergedPdfButton": "Export Converged PDF"
  }
}
```

Naming pattern: `{mode}CsvButton` / `{mode}PdfButton`
So three-tier keys should be: `threeTierCsvButton`, `threeTierPdfButton`

Existing three-tier keys already present (from Phase 19):
```json
{
  "mode": { "threeTier": "Three-Tier" },
  "threeTier": {
    "heading": "Three-Tier Sizing Parameters",
    "accessModel": "Access Switch Model",
    "aggregationModel": "Aggregation Switch Model",
    "coreModel": "Core Switch Model",
    ...
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three-tier export i18n keys to all 4 locales and audit</name>
  <files>
    src/i18n/locales/en/translation.json,
    src/i18n/locales/fr/translation.json,
    src/i18n/locales/de/translation.json,
    src/i18n/locales/it/translation.json
  </files>
  <read_first>
    src/i18n/locales/en/translation.json,
    src/i18n/locales/fr/translation.json,
    src/i18n/locales/de/translation.json,
    src/i18n/locales/it/translation.json
  </read_first>
  <action>
    Add the following keys to the `export` section of each locale file, placed after `convergedPdfButton` for consistent ordering:

    **English (en/translation.json):**
    ```json
    "threeTierCsvButton": "Export Three-Tier CSV",
    "threeTierPdfButton": "Export Three-Tier PDF"
    ```

    **French (fr/translation.json):**
    ```json
    "threeTierCsvButton": "Exporter CSV Trois niveaux",
    "threeTierPdfButton": "Exporter PDF Trois niveaux"
    ```

    **German (de/translation.json):**
    ```json
    "threeTierCsvButton": "Drei-Schicht CSV exportieren",
    "threeTierPdfButton": "Drei-Schicht PDF exportieren"
    ```

    **Italian (it/translation.json):**
    ```json
    "threeTierCsvButton": "Esporta CSV Tre livelli",
    "threeTierPdfButton": "Esporta PDF Tre livelli"
    ```

    After adding keys, perform a full i18n audit:
    1. Verify all `threeTier.*` keys exist in all 4 locales (heading, accessModel, aggregationModel, coreModel, etc.)
    2. Verify `mode.threeTier` exists in all 4 locales
    3. Verify `export.threeTierCsvButton` and `export.threeTierPdfButton` exist in all 4 locales
    4. Verify `converged.topologySelector`, `converged.topologyLeafSpine`, `converged.topologyThreeTier` exist in all 4 locales
    5. If ANY key is missing from any locale, add it with correct translation

    Also add three-tier specific BOM/export PDF labels if not already present in the `threeTier` section. Check if these exist and add if missing:
    ```json
    "accessToAggrOversubLabel": "Access-Aggregation Oversubscription",
    "aggrToCoreOversubLabel": "Aggregation-Core Oversubscription"
    ```
    (With translations for FR/DE/IT)

    Run `rtk vitest run` to verify no regressions. Run `rtk tsc --noEmit` for type safety.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/network-sizer && rtk vitest run && rtk tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "threeTierCsvButton" src/i18n/locales/en/translation.json
    - grep -q "threeTierPdfButton" src/i18n/locales/en/translation.json
    - grep -q "threeTierCsvButton" src/i18n/locales/fr/translation.json
    - grep -q "threeTierPdfButton" src/i18n/locales/fr/translation.json
    - grep -q "threeTierCsvButton" src/i18n/locales/de/translation.json
    - grep -q "threeTierPdfButton" src/i18n/locales/de/translation.json
    - grep -q "threeTierCsvButton" src/i18n/locales/it/translation.json
    - grep -q "threeTierPdfButton" src/i18n/locales/it/translation.json
  </acceptance_criteria>
  <done>All 4 locale files contain threeTierCsvButton and threeTierPdfButton. Full i18n audit passes -- no missing three-tier keys in any locale. All tests pass.</done>
</task>

</tasks>

<verification>
1. `rtk vitest run` -- all tests pass
2. `rtk tsc --noEmit` -- no TypeScript errors
3. Each locale file has threeTierCsvButton and threeTierPdfButton in the export section
4. All threeTier.* keys present in all 4 locales
</verification>

<success_criteria>
- export.threeTierCsvButton and export.threeTierPdfButton exist in EN, FR, DE, IT
- All threeTier.* keys (heading, accessModel, aggregationModel, coreModel, etc.) exist in all 4 locales
- All converged topology selector keys exist in all 4 locales
- No missing i18n keys for any three-tier feature
- All tests pass, TSC clean
</success_criteria>

<output>
After completion, create `.planning/phases/20-three-tier-export-i18n/20-02-SUMMARY.md`
</output>
