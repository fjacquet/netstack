---
phase: 08-fc-catalog-and-schema-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/catalog/fc-types.ts
  - src/domain/catalog/brocade.ts
  - src/domain/catalog/brocade.test.ts
autonomous: true
requirements:
  - FC-01
  - FC-02
  - FC-03
  - FC-04

must_haves:
  truths:
    - "FC_SWITCH_CATALOG contains exactly 9 models: G710, G720, G730, X7-4, X7-8, 7850, G820, X8-4, X8-8"
    - "Every fixed-port switch (G710, G720, G730, G820) has basePorts < totalPorts and podLicenseUnit > 0"
    - "Every director switch (X7-4, X7-8, X8-4, X8-8) has podLicenseUnit=0 and formFactor='director'"
    - "7850 has role='extension', totalPorts=24, basePorts=24, maxIslPorts=18"
    - "FC_OPTICS_CATALOG contains 3 entries, all with protocol='fibre-channel'"
    - "TypeScript strict mode passes: npx tsc --noEmit exits 0"
    - "All catalog assertions in brocade.test.ts pass: npx vitest run exits 0"
  artifacts:
    - path: "src/domain/catalog/fc-types.ts"
      provides: "FCSwitchSpec and FCOpticsSpec interfaces"
      exports: ["FCSwitchSpec", "FCOpticsSpec"]
    - path: "src/domain/catalog/brocade.ts"
      provides: "FC_SWITCH_CATALOG and FC_OPTICS_CATALOG constants"
      exports: ["FC_SWITCH_CATALOG", "FCSwitchModelId", "FC_OPTICS_CATALOG", "FCOpticsId"]
    - path: "src/domain/catalog/brocade.test.ts"
      provides: "Unit tests covering all 9 models and 3 optics"
  key_links:
    - from: "src/domain/catalog/brocade.ts"
      to: "src/domain/catalog/fc-types.ts"
      via: "import type { FCSwitchSpec, FCOpticsSpec }"
      pattern: "import type.*FCSwitchSpec.*from.*fc-types"
    - from: "src/domain/catalog/brocade.ts"
      to: "FC_SWITCH_CATALOG entries"
      via: "as const satisfies Record<string, FCSwitchSpec>"
      pattern: "as const satisfies Record"
---

<objective>
Create the Fibre Channel hardware catalog: FCSwitchSpec and FCOpticsSpec interfaces in fc-types.ts, then FC_SWITCH_CATALOG (9 Brocade models) and FC_OPTICS_CATALOG (3 entries) in brocade.ts, all verified by typed unit tests in brocade.test.ts.

Purpose: Every downstream phase (FC engine, FC stores, FC UI) depends on these catalog constants. The POD licensing fields (basePorts, podLicenseUnit, totalPorts) must be correct before Phase 10 engine is written — retrofitting them breaks schema compatibility.

Output: fc-types.ts, brocade.ts, brocade.test.ts — all green with npx vitest run.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-fc-catalog-and-schema-foundation/08-RESEARCH.md

@src/domain/catalog/types.ts
@src/domain/catalog/hardware.ts
@src/domain/catalog/hardware.test.ts
</context>

<interfaces>
<!-- Existing interfaces to replicate pattern from — do NOT import FC items from these files -->

From src/domain/catalog/types.ts:
```typescript
export interface SwitchSpec {
  modelId: string;
  role: 'leaf' | 'spine' | 'oob';
  downlinkPorts: number;
  downlinkSpeedGbE: number;
  uplinkPorts: number;
  uplinkSpeedGbE?: number;
  additionalUplinkPorts?: number;
  maxPowerW: number;
  typicalPowerW?: number;
  switchingCapacityTbps?: number;
}
```

From src/domain/catalog/hardware.ts:
```typescript
export const SWITCH_CATALOG = {
  'S5248F-ON': { modelId: 'S5248F-ON', role: 'leaf', ... },
} as const satisfies Record<string, SwitchSpec>;

export type SwitchModelId = keyof typeof SWITCH_CATALOG;
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Define FCSwitchSpec and FCOpticsSpec interfaces</name>
  <files>src/domain/catalog/fc-types.ts</files>

  <read_first>
    - src/domain/catalog/types.ts (existing SwitchSpec pattern to replicate)
    - .planning/phases/08-fc-catalog-and-schema-foundation/08-RESEARCH.md (Pattern 2: FCSwitchSpec Interface section)
  </read_first>

  <behavior>
    - FCSwitchSpec interface exported from fc-types.ts with fields: modelId (string), generation (7|8), speedGbps (32|64|128), totalPorts (number), basePorts (number), podLicenseUnit (number), maxIslPorts (number), uHeight (number), maxPowerW (number), typicalPowerW? (number), role ('edge'|'director'|'extension'), formFactor ('fixed'|'director'), sfpDDPorts? (number), bladeSlotsCount? (number), portsPerBlade? (number)
    - FCOpticsSpec interface exported with fields: protocol ('fibre-channel' as literal — never 'ethernet'), speedGbps (32|64|128), formFactor ('SFP28'|'SFP+'|'SFP-DD'|'QSFP'), wavelengthNm (number), connectorType ('LC-duplex'|'SFP-DD'), maxDistanceM (number), useCase (string)
    - File contains ONLY interface declarations — no constants, no imports from non-type sources
    - File has NO imports from hardware.ts, bom.ts, or any Ethernet domain file
  </behavior>

  <action>
Create `src/domain/catalog/fc-types.ts` with exactly these two exported interfaces. Use JSDoc comments mirroring types.ts style. The `protocol: 'fibre-channel'` field on FCOpticsSpec is a structural discriminant — document it as such in its JSDoc. The `podLicenseUnit: number` field on FCSwitchSpec must be documented: value is 0 for directors (blade-based, not port POD), > 0 for fixed-port switches.

Exact FCSwitchSpec interface:
```typescript
export interface FCSwitchSpec {
  modelId: string;
  generation: 7 | 8;
  speedGbps: 32 | 64 | 128;
  totalPorts: number;
  basePorts: number;
  podLicenseUnit: number;  // 0 for directors (blade-based); >0 for fixed switches
  maxIslPorts: number;
  uHeight: number;
  maxPowerW: number;
  typicalPowerW?: number;
  role: 'edge' | 'director' | 'extension';
  formFactor: 'fixed' | 'director';
  sfpDDPorts?: number;
  bladeSlotsCount?: number;
  portsPerBlade?: number;
}
```

Exact FCOpticsSpec interface:
```typescript
export interface FCOpticsSpec {
  protocol: 'fibre-channel';  // discriminant — never 'ethernet'
  speedGbps: 32 | 64 | 128;
  formFactor: 'SFP28' | 'SFP+' | 'SFP-DD' | 'QSFP';
  wavelengthNm: number;
  connectorType: 'LC-duplex' | 'SFP-DD';
  maxDistanceM: number;
  useCase: string;
}
```
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "fc-types" || echo "fc-types: no TS errors"</automated>
  </verify>

  <acceptance_criteria>
    - src/domain/catalog/fc-types.ts exists
    - File exports `FCSwitchSpec` (grep: `export interface FCSwitchSpec`)
    - File exports `FCOpticsSpec` (grep: `export interface FCOpticsSpec`)
    - FCSwitchSpec has `podLicenseUnit: number` field (grep: `podLicenseUnit`)
    - FCSwitchSpec has `basePorts: number` field (grep: `basePorts`)
    - FCSwitchSpec has `totalPorts: number` field (grep: `totalPorts`)
    - FCOpticsSpec has `protocol: 'fibre-channel'` (grep: `protocol:.*fibre-channel`)
    - No imports from hardware.ts or types.ts in this file (grep: no `from.*hardware` or `from.*types`)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>fc-types.ts exists, exports both interfaces with all required fields, TypeScript compiles clean.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement FC_SWITCH_CATALOG (9 models) and FC_OPTICS_CATALOG (3 entries) with tests</name>
  <files>src/domain/catalog/brocade.ts, src/domain/catalog/brocade.test.ts</files>

  <read_first>
    - src/domain/catalog/fc-types.ts (interfaces just created — use these exact field names)
    - src/domain/catalog/hardware.ts (as const satisfies pattern to replicate)
    - src/domain/catalog/hardware.test.ts (test structure to replicate)
    - .planning/phases/08-fc-catalog-and-schema-foundation/08-RESEARCH.md (Verified Brocade FC Switch Specifications table)
  </read_first>

  <behavior>
    TDD cycle: Write tests first (RED), then implement catalog (GREEN).

    RED — brocade.test.ts assertions that must fail before brocade.ts exists:
    - FC_SWITCH_CATALOG contains exactly 9 models
    - FC_SWITCH_CATALOG keys include: 'G710', 'G720', 'G730', 'X7-4', 'X7-8', '7850', 'G820', 'X8-4', 'X8-8'
    - G710: totalPorts=24, basePorts=8, podLicenseUnit=8, speedGbps=64, generation=7, role='edge', formFactor='fixed', uHeight=1, maxPowerW=150
    - G720: totalPorts=64, basePorts=24, podLicenseUnit=8, speedGbps=64, generation=7, role='edge', formFactor='fixed', uHeight=1
    - G720: basePorts(24) < totalPorts(64) — POD model assertion
    - G730: totalPorts=128, basePorts=48, speedGbps=64, generation=7, role='edge', formFactor='fixed', uHeight=2, maxPowerW=969
    - X7-4: totalPorts=256, basePorts=256, podLicenseUnit=0, generation=7, role='director', formFactor='director', bladeSlotsCount=4, portsPerBlade=64
    - X7-8: totalPorts=512, basePorts=512, podLicenseUnit=0, generation=7, role='director', formFactor='director', bladeSlotsCount=8
    - 7850: totalPorts=24, basePorts=24, podLicenseUnit=0, role='extension', formFactor='fixed', maxIslPorts=18, speedGbps=64, generation=7, uHeight=1
    - G820: totalPorts=56, basePorts=24, podLicenseUnit=8, speedGbps=128, generation=8, role='edge', formFactor='fixed', uHeight=1, maxPowerW=650
    - X8-4: totalPorts=192, basePorts=192, podLicenseUnit=0, generation=8, role='director', formFactor='director', bladeSlotsCount=4, portsPerBlade=48
    - X8-8: totalPorts=384, basePorts=384, podLicenseUnit=0, generation=8, role='director', formFactor='director', bladeSlotsCount=8, portsPerBlade=48
    - Every fixed switch: basePorts < totalPorts (loop assertion)
    - Every fixed switch: podLicenseUnit > 0 (loop assertion)
    - Every director: podLicenseUnit === 0 (loop assertion)
    - FC_OPTICS_CATALOG contains exactly 3 entries
    - All 3 optics have protocol === 'fibre-channel'
    - 'FC-32G-SW-SFP28': speedGbps=32, formFactor='SFP28'
    - 'FC-64G-SW-SFP+': speedGbps=64, formFactor='SFP+'
    - 'FC-128G-SW-SFP+': speedGbps=128, formFactor='SFP+'
  </behavior>

  <action>
STEP 1 — Write brocade.test.ts (RED):
Create `src/domain/catalog/brocade.test.ts` with all assertions listed in behavior block. Use same describe/it/expect pattern as hardware.test.ts. Run `npx vitest run src/domain/catalog/brocade.test.ts` — must FAIL (file not found / import errors).

STEP 2 — Implement brocade.ts (GREEN):
Create `src/domain/catalog/brocade.ts` using `as const satisfies Record<string, FCSwitchSpec>` pattern. Import `type { FCSwitchSpec, FCOpticsSpec }` from `./fc-types`.

Exact catalog values from research (verified against Broadcom TechDocs):

FC_SWITCH_CATALOG entries:
- 'G710': modelId='G710', generation=7, speedGbps=64, totalPorts=24, basePorts=8, podLicenseUnit=8, maxIslPorts=6, uHeight=1, maxPowerW=150, role='edge', formFactor='fixed'
- 'G720': modelId='G720', generation=7, speedGbps=64, totalPorts=64, basePorts=24, podLicenseUnit=8, maxIslPorts=16, uHeight=1, maxPowerW=700, typicalPowerW=349, role='edge', formFactor='fixed', sfpDDPorts=8
- 'G730': modelId='G730', generation=7, speedGbps=64, totalPorts=128, basePorts=48, podLicenseUnit=8, maxIslPorts=32, uHeight=2, maxPowerW=1100, typicalPowerW=969, role='edge', formFactor='fixed', sfpDDPorts=16
- 'X7-4': modelId='X7-4', generation=7, speedGbps=64, totalPorts=256, basePorts=256, podLicenseUnit=0, maxIslPorts=16, uHeight=8, maxPowerW=2000, role='director', formFactor='director', bladeSlotsCount=4, portsPerBlade=64
  // Note: Lenovo Press cites 265 max including 9 ICL ports; 256 = 4×64 data ports used for sizing
- 'X7-8': modelId='X7-8', generation=7, speedGbps=64, totalPorts=512, basePorts=512, podLicenseUnit=0, maxIslPorts=32, uHeight=14, maxPowerW=4000, role='director', formFactor='director', bladeSlotsCount=8, portsPerBlade=64
- '7850': modelId='7850', generation=7, speedGbps=64, totalPorts=24, basePorts=24, podLicenseUnit=0, maxIslPorts=18, uHeight=1, maxPowerW=200, role='extension', formFactor='fixed'
  // 16 physical FC ports (8×SFP-DD + 8×SFP+ = 24 addressable); 18 GE WAN ports (role: extension)
- 'G820': modelId='G820', generation=8, speedGbps=128, totalPorts=56, basePorts=24, podLicenseUnit=8, maxIslPorts=16, uHeight=1, maxPowerW=650, typicalPowerW=336, role='edge', formFactor='fixed'
- 'X8-4': modelId='X8-4', generation=8, speedGbps=128, totalPorts=192, basePorts=192, podLicenseUnit=0, maxIslPorts=16, uHeight=9, maxPowerW=2000, role='director', formFactor='director', bladeSlotsCount=4, portsPerBlade=48
  // X8-4: 4 × FC128-48 blades = 192 ports. Power estimated — marked with TODO comment
- 'X8-8': modelId='X8-8', generation=8, speedGbps=128, totalPorts=384, basePorts=384, podLicenseUnit=0, maxIslPorts=32, uHeight=14, maxPowerW=4000, role='director', formFactor='director', bladeSlotsCount=8, portsPerBlade=48
  // X8-8: 8 × FC128-48 blades = 384 ports. Power estimated — marked with TODO comment

FC_OPTICS_CATALOG entries (as const — not satisfies, since FCOpticsSpec is inline):
- 'FC-32G-SW-SFP28': { protocol: 'fibre-channel' as const, speedGbps: 32, formFactor: 'SFP28', wavelengthNm: 850, connectorType: 'LC-duplex', maxDistanceM: 100, useCase: '32G short-wavelength, OM4 multimode, intra-rack' }
- 'FC-64G-SW-SFP+': { protocol: 'fibre-channel' as const, speedGbps: 64, formFactor: 'SFP+', wavelengthNm: 850, connectorType: 'LC-duplex', maxDistanceM: 100, useCase: '64G short-wavelength, OM4 multimode, G720/G730' }
- 'FC-128G-SW-SFP+': { protocol: 'fibre-channel' as const, speedGbps: 128, formFactor: 'SFP+', wavelengthNm: 850, connectorType: 'LC-duplex', maxDistanceM: 100, useCase: '128G short-wavelength, OM4 multimode, G820' }

Also export:
- `export type FCSwitchModelId = keyof typeof FC_SWITCH_CATALOG;`
- `export type FCOpticsId = keyof typeof FC_OPTICS_CATALOG;`

STEP 3 — Run tests (must pass GREEN):
`npx vitest run src/domain/catalog/brocade.test.ts`
  </action>

  <verify>
    <automated>npx vitest run src/domain/catalog/brocade.test.ts 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/domain/catalog/brocade.ts exists
    - src/domain/catalog/brocade.test.ts exists
    - FC_SWITCH_CATALOG exported from brocade.ts (grep: `export const FC_SWITCH_CATALOG`)
    - FC_OPTICS_CATALOG exported from brocade.ts (grep: `export const FC_OPTICS_CATALOG`)
    - FCSwitchModelId exported (grep: `export type FCSwitchModelId`)
    - FCOpticsId exported (grep: `export type FCOpticsId`)
    - brocade.ts uses `as const satisfies Record<string, FCSwitchSpec>` (grep: `as const satisfies`)
    - brocade.ts imports from './fc-types' (grep: `from.*fc-types`)
    - brocade.ts does NOT import from './hardware' or './cables' (grep: no `from.*hardware`, no `from.*cables`)
    - `npx vitest run src/domain/catalog/brocade.test.ts` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>FC_SWITCH_CATALOG (9 models) and FC_OPTICS_CATALOG (3 entries) exist with correct specs, all unit tests pass, TypeScript strict mode clean.</done>
</task>

</tasks>

<verification>
Full verification commands (run in order):
1. `npx vitest run src/domain/catalog/brocade.test.ts` — all catalog tests green
2. `npx tsc --noEmit` — no TypeScript errors
3. `npx vitest run` — full suite still green (no regressions)
</verification>

<success_criteria>
- fc-types.ts exports FCSwitchSpec and FCOpticsSpec with all required fields including POD licensing fields (basePorts, totalPorts, podLicenseUnit)
- brocade.ts exports FC_SWITCH_CATALOG with all 9 models at correct specs (verified by tests)
- brocade.ts exports FC_OPTICS_CATALOG with 3 entries all having protocol='fibre-channel'
- brocade.test.ts covers every model (G710, G720, G730, X7-4, X7-8, 7850, G820, X8-4, X8-8) and every optics entry
- npx vitest run exits 0
- npx tsc --noEmit exits 0
- No Ethernet catalog files (hardware.ts, cables.ts, types.ts) were modified
</success_criteria>

<output>
After completion, create `.planning/phases/08-fc-catalog-and-schema-foundation/08-01-SUMMARY.md`
</output>
