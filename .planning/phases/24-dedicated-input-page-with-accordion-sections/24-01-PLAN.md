---
phase: 24-dedicated-input-page-with-accordion-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main.tsx
  - src/components/ui/accordion.tsx
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/it/translation.json
  - src/features/input/InputPage.test.tsx
  - src/features/input/EthInputAccordion.test.tsx
  - src/features/input/FCInputAccordion.test.tsx
  - src/components/TopBar.test.tsx
  - src/App.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - COMPAT

must_haves:
  truths:
    - "react-router-dom is installed and HashRouter wraps the app root"
    - "shadcn accordion component exists at src/components/ui/accordion.tsx"
    - "All 4 locale files contain nav.configure, nav.results, nav.topology, nav.rack, input.pageTitle, input.pageDescription, input.section.* keys"
    - "Wave 0 test stubs exist for InputPage, EthInputAccordion, FCInputAccordion, TopBar, and App"
  artifacts:
    - path: "src/components/ui/accordion.tsx"
      provides: "Accordion, AccordionItem, AccordionTrigger, AccordionContent components"
      contains: "AccordionTrigger"
    - path: "src/main.tsx"
      provides: "HashRouter wrapping App at root"
      contains: "HashRouter"
    - path: "src/features/input/InputPage.test.tsx"
      provides: "Wave 0 test stub for InputPage"
      contains: "describe"
    - path: "src/features/input/EthInputAccordion.test.tsx"
      provides: "Wave 0 test stub for EthInputAccordion"
      contains: "describe"
    - path: "src/features/input/FCInputAccordion.test.tsx"
      provides: "Wave 0 test stub for FCInputAccordion"
      contains: "describe"
    - path: "src/components/TopBar.test.tsx"
      provides: "Wave 0 test stub for TopBar Configure button"
      contains: "describe"
    - path: "src/App.test.tsx"
      provides: "Wave 0 test stub for NavLink end prop"
      contains: "describe"
  key_links:
    - from: "src/main.tsx"
      to: "react-router-dom"
      via: "HashRouter import"
      pattern: "import.*HashRouter.*from.*react-router-dom"
---

<objective>
Install react-router-dom and shadcn accordion, wrap App in HashRouter, add all i18n keys for Phase 24, and create Wave 0 test stubs.

Purpose: Establish the routing infrastructure and UI component foundation that Plan 02 depends on. Without HashRouter at the root, no component can use useNavigate or NavLink. Without accordion.tsx, accordion forms cannot be built. Without i18n keys, no labels are available.

Output: HashRouter wrapping App, accordion.tsx component, 4 locale files updated, 5 test stub files created.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dedicated-input-page-with-accordion-sections/24-CONTEXT.md
@.planning/phases/24-dedicated-input-page-with-accordion-sections/24-RESEARCH.md
@.planning/phases/24-dedicated-input-page-with-accordion-sections/24-UI-SPEC.md
@.planning/phases/24-dedicated-input-page-with-accordion-sections/24-VALIDATION.md

<interfaces>
<!-- Current main.tsx (11 lines) — will be wrapped with HashRouter -->
From src/main.tsx:
```tsx
import './i18n/index'
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App'
import './index.css'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
)
```

<!-- Current App.tsx imports — these will be modified in Plan 02 -->
From src/App.tsx:
```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
```

<!-- TopBar props interface — unchanged, Plan 02 adds router hooks -->
From src/components/TopBar.tsx:
```tsx
interface TopBarProps {
  mode: 'ethernet' | 'fc' | 'converged'
  onModeChange: (m: 'ethernet' | 'fc' | 'converged') => void
  profilesOpen: boolean
  onToggleProfiles: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add HashRouter, add shadcn accordion</name>
  <files>package.json, src/main.tsx, src/components/ui/accordion.tsx</files>
  <read_first>
    - src/main.tsx (current state before modification)
    - package.json (verify react-router-dom not yet installed)
  </read_first>
  <action>
1. Run `npm install react-router-dom` to install React Router v7.
2. Run `npx shadcn add accordion` to add the Radix-based accordion component — this creates `src/components/ui/accordion.tsx` automatically.
3. Modify `src/main.tsx` to wrap `<App />` with `<HashRouter>`:

```tsx
import './i18n/index'
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { HashRouter } from 'react-router-dom'
import App from './App'
import './index.css'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <HashRouter>
      <App />
    </HashRouter>
  </StrictMode>
)
```

CRITICAL: Do NOT add a `basename` prop to HashRouter — hash routing does not use basename.
CRITICAL: `import './i18n/index'` MUST remain the first import (before App) — synchronous i18n init.
  </action>
  <verify>
    <automated>grep -q "HashRouter" src/main.tsx && grep -q "react-router-dom" package.json && test -f src/components/ui/accordion.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.tsx` contains `import { HashRouter } from 'react-router-dom'`
    - `src/main.tsx` contains `<HashRouter>` wrapping `<App />`
    - `src/main.tsx` does NOT contain `basename` prop
    - `package.json` has `"react-router-dom"` in dependencies
    - `src/components/ui/accordion.tsx` exists and exports `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent`
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>react-router-dom installed, HashRouter wraps App in main.tsx, accordion.tsx component exists</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys to all 4 locale files</name>
  <files>src/i18n/locales/en/translation.json, src/i18n/locales/fr/translation.json, src/i18n/locales/de/translation.json, src/i18n/locales/it/translation.json</files>
  <read_first>
    - src/i18n/locales/en/translation.json (current keys)
    - src/i18n/locales/fr/translation.json (current keys)
    - src/i18n/locales/de/translation.json (current keys)
    - src/i18n/locales/it/translation.json (current keys)
  </read_first>
  <action>
Add these new keys to ALL 4 locale files. Add them as new top-level namespaces `nav` and `input` (peers of existing `app`, `tabs`, `sizing`, etc.). Also update the existing `results.emptyBody` key.

**English (en/translation.json):**
```json
"nav": {
  "configure": "Configure Inputs",
  "results": "Results",
  "topology": "Topology",
  "rack": "Rack Elevation"
},
"input": {
  "pageTitle": "Network Configuration",
  "pageDescription": "Configure your network parameters. Results update automatically.",
  "section": {
    "rackConfig": "Rack Configuration",
    "switchSelection": "Switch Selection",
    "advanced": "Advanced",
    "fabricConfig": "Fabric Configuration",
    "ethernetSwitches": "Ethernet Switches",
    "fcFabric": "FC Fabric"
  }
}
```
Update existing: `"results.emptyBody": "Configure your network in the Inputs page, then return here to see your BOM."`

**French (fr/translation.json):**
```json
"nav": {
  "configure": "Configurer",
  "results": "Résultats",
  "topology": "Topologie",
  "rack": "Élévation de baie"
},
"input": {
  "pageTitle": "Configuration réseau",
  "pageDescription": "Configurez vos paramètres réseau. Les résultats se mettent à jour automatiquement.",
  "section": {
    "rackConfig": "Configuration des baies",
    "switchSelection": "Sélection des commutateurs",
    "advanced": "Avancé",
    "fabricConfig": "Configuration du fabric",
    "ethernetSwitches": "Commutateurs Ethernet",
    "fcFabric": "Fabric FC"
  }
}
```
Update existing: `"results.emptyBody": "Configurez votre réseau dans la page Entrées, puis revenez ici pour voir votre nomenclature."`

**German (de/translation.json):**
```json
"nav": {
  "configure": "Konfigurieren",
  "results": "Ergebnisse",
  "topology": "Topologie",
  "rack": "Rack-Ansicht"
},
"input": {
  "pageTitle": "Netzwerkkonfiguration",
  "pageDescription": "Netzwerkparameter konfigurieren. Ergebnisse werden automatisch aktualisiert.",
  "section": {
    "rackConfig": "Rack-Konfiguration",
    "switchSelection": "Switch-Auswahl",
    "advanced": "Erweitert",
    "fabricConfig": "Fabric-Konfiguration",
    "ethernetSwitches": "Ethernet-Switches",
    "fcFabric": "FC-Fabric"
  }
}
```
Update existing: `"results.emptyBody": "Konfigurieren Sie Ihr Netzwerk auf der Eingabeseite und kehren Sie dann hierher zurück, um Ihre Stückliste zu sehen."`

**Italian (it/translation.json):**
```json
"nav": {
  "configure": "Configura",
  "results": "Risultati",
  "topology": "Topologia",
  "rack": "Elevazione rack"
},
"input": {
  "pageTitle": "Configurazione di rete",
  "pageDescription": "Configura i parametri di rete. I risultati si aggiornano automaticamente.",
  "section": {
    "rackConfig": "Configurazione rack",
    "switchSelection": "Selezione switch",
    "advanced": "Avanzate",
    "fabricConfig": "Configurazione fabric",
    "ethernetSwitches": "Switch Ethernet",
    "fcFabric": "Fabric FC"
  }
}
```
Update existing: `"results.emptyBody": "Configura la tua rete nella pagina Ingressi, poi torna qui per vedere la tua distinta materiali."`

CRITICAL: Place `nav` and `input` namespaces alphabetically among existing top-level keys. Do NOT remove any existing keys.
  </action>
  <verify>
    <automated>grep -q '"nav"' src/i18n/locales/en/translation.json && grep -q '"input"' src/i18n/locales/en/translation.json && grep -q '"nav"' src/i18n/locales/fr/translation.json && grep -q '"nav"' src/i18n/locales/de/translation.json && grep -q '"nav"' src/i18n/locales/it/translation.json && grep -q "Configure your network in the Inputs page" src/i18n/locales/en/translation.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 locale files contain `"nav"` top-level key with `configure`, `results`, `topology`, `rack` sub-keys
    - All 4 locale files contain `"input"` top-level key with `pageTitle`, `pageDescription`, and `section` sub-keys
    - `section` sub-keys include: `rackConfig`, `switchSelection`, `advanced`, `fabricConfig`, `ethernetSwitches`, `fcFabric`
    - `results.emptyBody` updated in all 4 locale files (no longer contains "Fill in the form on the left")
    - EN locale `nav.configure` equals `"Configure Inputs"`
    - FR locale `nav.configure` equals `"Configurer"`
    - DE locale `nav.configure` equals `"Konfigurieren"`
    - IT locale `nav.configure` equals `"Configura"`
    - All existing keys preserved (none removed)
    - JSON is valid (no syntax errors)
  </acceptance_criteria>
  <done>All 4 locale files have nav.*, input.*, and updated results.emptyBody keys</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test stubs per VALIDATION.md</name>
  <files>src/features/input/InputPage.test.tsx, src/features/input/EthInputAccordion.test.tsx, src/features/input/FCInputAccordion.test.tsx, src/components/TopBar.test.tsx, src/App.test.tsx</files>
  <read_first>
    - .planning/phases/24-dedicated-input-page-with-accordion-sections/24-VALIDATION.md (Wave 0 requirements)
    - vite.config.ts (test environment configuration)
  </read_first>
  <action>
Create 5 test stub files. These are Wave 0 stubs — they define test structure and expectations but use `it.todo()` for tests that depend on components not yet created (Plan 02). Each file must import vitest functions and set up the test describe block.

1. **`src/features/input/InputPage.test.tsx`** (UI-01):
```tsx
import { describe, it, expect } from 'vitest'

describe('InputPage', () => {
  it.todo('renders EthInputAccordion when mode=ethernet')
  it.todo('renders FCInputAccordion when mode=fc')
  it.todo('renders ConvergedInputAccordion when mode=converged')
  it.todo('displays page title from i18n input.pageTitle')
  it.todo('wraps accordion in Card component')
})
```

2. **`src/features/input/EthInputAccordion.test.tsx`** (UI-02):
```tsx
import { describe, it, expect } from 'vitest'

describe('EthInputAccordion', () => {
  it.todo('renders 3 accordion sections: rack-config, switch-selection, advanced')
  it.todo('rack-config section is open by default')
  it.todo('syncs form changes to inputStore via setInput')
  it.todo('shows Clos fields when topology is spine-leaf')
  it.todo('shows Three-Tier fields when topology is three-tier')
  it.todo('includes portsPerServerFrontend field')
  it.todo('includes portsPerServerBackend field')
})
```

3. **`src/features/input/FCInputAccordion.test.tsx`** (UI-03):
```tsx
import { describe, it, expect } from 'vitest'

describe('FCInputAccordion', () => {
  it.todo('renders 3 accordion sections: rack-config, fabric-config, advanced')
  it.todo('rack-config section is open by default')
  it.todo('syncs form changes to fcInputStore via setInput')
  it.todo('includes ISL ports field in advanced section')
})
```

4. **`src/components/TopBar.test.tsx`** (UI-04):
```tsx
import { describe, it, expect } from 'vitest'

describe('TopBar', () => {
  it.todo('renders Configure button with SlidersHorizontal icon')
  it.todo('Configure button navigates to /input on click')
  it.todo('Configure button shows variant=secondary when on /input route')
  it.todo('Configure button shows variant=ghost when not on /input route')
})
```

5. **`src/App.test.tsx`** (UI-05):
```tsx
import { describe, it, expect } from 'vitest'

describe('App routing', () => {
  it.todo('renders Routes with /input, /, /topology, /rack paths')
  it.todo('NavLink for / uses end prop')
  it.todo('NavLink strip has aria-label="page navigation"')
  it.todo('FC mode hides rack NavLink')
})
```

IMPORTANT: Create the `src/features/input/` directory if it does not exist (it is new in Phase 24).
IMPORTANT: Use `it.todo()` (not `it.skip()`) — todo tests show as pending in vitest output without requiring implementation.
  </action>
  <verify>
    <automated>npx vitest run src/features/input/InputPage.test.tsx src/features/input/EthInputAccordion.test.tsx src/features/input/FCInputAccordion.test.tsx src/components/TopBar.test.tsx src/App.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/input/` directory exists
    - `src/features/input/InputPage.test.tsx` exists and contains `describe('InputPage'`
    - `src/features/input/EthInputAccordion.test.tsx` exists and contains `describe('EthInputAccordion'`
    - `src/features/input/FCInputAccordion.test.tsx` exists and contains `describe('FCInputAccordion'`
    - `src/components/TopBar.test.tsx` exists and contains `describe('TopBar'`
    - `src/App.test.tsx` exists and contains `describe('App routing'`
    - All 5 test files use `it.todo()` (not `it.skip`)
    - `npx vitest run` completes without errors (todo tests show as pending, not failures)
    - Existing domain tests still pass: `npx vitest run src/domain/`
  </acceptance_criteria>
  <done>5 Wave 0 test stub files created with todo tests, vitest runs without errors, domain regression suite passes</done>
</task>

</tasks>

<verification>
1. `grep -q "HashRouter" src/main.tsx` — HashRouter installed in main.tsx
2. `test -f src/components/ui/accordion.tsx` — Accordion component exists
3. `grep -q '"react-router-dom"' package.json` — react-router-dom in dependencies
4. `grep -q '"nav"' src/i18n/locales/en/translation.json` — i18n nav keys present
5. `grep -q '"input"' src/i18n/locales/en/translation.json` — i18n input keys present
6. `npx vitest run src/domain/` — Domain regression suite passes
7. `npx tsc --noEmit` — TypeScript compilation succeeds
</verification>

<success_criteria>
- react-router-dom installed in package.json
- HashRouter wraps App in main.tsx (no basename prop)
- accordion.tsx exists in src/components/ui/
- All 4 locale files have nav.* and input.* keys
- results.emptyBody updated in all 4 locales
- 5 test stub files created with it.todo() tests
- All existing tests pass (npx vitest run)
- TypeScript compiles (npx tsc --noEmit)
</success_criteria>

<output>
After completion, create `.planning/phases/24-dedicated-input-page-with-accordion-sections/24-01-SUMMARY.md`
</output>
