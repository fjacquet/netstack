---
phase: 28-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/export/exportCsv.ts
  - src/features/export/exportCsv.test.ts
  - src/features/export/exportThreeTierCsv.ts
  - src/features/export/exportThreeTierCsv.test.ts
  - src/features/export/exportFCCsv.ts
  - src/features/export/exportFCCsv.test.ts
  - src/features/export/pdf/BOMPage.tsx
  - src/features/export/pdf/ThreeTierBOMPage.tsx
  - src/features/export/pdf/FCBOMPage.tsx
autonomous: true
requirements: [EXP-05, EXP-06]

must_haves:
  truths:
    - "Exported CSV contains a Cable Schedule section with per-link-type rows showing qty and SKU"
    - "Exported PDF contains a Cable Schedule subsection with a table of link types, quantities, and SKU lengths"
    - "Cable schedule rows only appear when cableSchedule/islCableLengthSkuM is present on BOM"
    - "FC CSV and PDF show ISL cable length from top-level islCableLengthSkuM, not nested cableSchedule"
    - "All CSV cable schedule rows have exactly 7 columns (no Converged export breakage)"
  artifacts:
    - path: "src/features/export/exportCsv.ts"
      provides: "Clos cable schedule CSV rows"
      contains: "Cable Schedule"
    - path: "src/features/export/exportThreeTierCsv.ts"
      provides: "Three-Tier cable schedule CSV rows"
      contains: "Cable Schedule"
    - path: "src/features/export/exportFCCsv.ts"
      provides: "FC ISL cable length CSV row"
      contains: "Cable Schedule"
    - path: "src/features/export/pdf/BOMPage.tsx"
      provides: "Clos cable schedule PDF section"
      contains: "Cable Schedule"
    - path: "src/features/export/pdf/ThreeTierBOMPage.tsx"
      provides: "Three-Tier cable schedule PDF section"
      contains: "Cable Schedule"
    - path: "src/features/export/pdf/FCBOMPage.tsx"
      provides: "FC ISL cable length PDF section"
      contains: "Cable Schedule"
  key_links:
    - from: "src/features/export/exportCsv.ts"
      to: "bom.cableSchedule"
      via: "null guard then field access"
      pattern: "bom\\.cableSchedule"
    - from: "src/features/export/exportFCCsv.ts"
      to: "bom.islCableLengthSkuM"
      via: "null guard on top-level field"
      pattern: "bom\\.islCableLengthSkuM"
    - from: "src/features/export/pdf/BOMPage.tsx"
      to: "bom.cableSchedule"
      via: "conditional JSX render"
      pattern: "bom\\.cableSchedule"
---

<objective>
Add cable schedule sections to all CSV and PDF exports across Clos, Three-Tier, and FC modes.

Purpose: Complete v6.0 Physical Planning milestone by exposing cable length schedule data (computed in Phase 26) in exported artifacts so procurement teams can order the right cable lengths.

Output: Updated CSV export functions emit cable schedule rows; updated PDF BOM page components render a cable schedule table. Converged export inherits changes automatically via delegation.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-export/28-RESEARCH.md

<interfaces>
<!-- BOM schemas with optional cableSchedule fields — executor must null-guard these -->

From src/domain/schemas/bom.ts (NetworkBOM):
```typescript
cableSchedule: z.object({
  serverLeafSkuM: z.number().int(),
  leafSpineSkuM: z.number().int(),
  vltSkuM: z.number().int(),
}).optional()
```

From src/domain/schemas/three-tier-bom.ts (ThreeTierBOM):
```typescript
cableSchedule: z.object({
  serverAccessSkuM: z.number().int(),
  accessAggregationSkuM: z.number().int(),
  aggregationCoreSkuM: z.number().int(),
}).optional()
```

From src/domain/schemas/fc-bom.ts (FCNetworkBOM):
```typescript
// TOP-LEVEL field, NOT nested under cableSchedule (ADR-0009)
islCableLengthSkuM: z.number().int().optional()
// Also: islCables: z.number().int().min(0) for qty
```

CSV 7-column header (fixed, shared across all modes):
```
Category,Model / Type,Role,Quantity,Unit,Connectivity,Notes
```

IMPORTANT: buildCsvRows() and buildThreeTierCsvRows() return string[] (already joined).
buildFCCsvRows() returns string[][] (raw arrays, joined later in buildFCCsvString).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSV cable schedule rows for Clos, Three-Tier, and FC</name>
  <files>
    src/features/export/exportCsv.ts,
    src/features/export/exportCsv.test.ts,
    src/features/export/exportThreeTierCsv.ts,
    src/features/export/exportThreeTierCsv.test.ts,
    src/features/export/exportFCCsv.ts,
    src/features/export/exportFCCsv.test.ts
  </files>
  <read_first>
    src/features/export/exportCsv.ts,
    src/features/export/exportCsv.test.ts,
    src/features/export/exportThreeTierCsv.ts,
    src/features/export/exportThreeTierCsv.test.ts,
    src/features/export/exportFCCsv.ts,
    src/features/export/exportFCCsv.test.ts,
    src/domain/schemas/bom.ts,
    src/domain/schemas/three-tier-bom.ts,
    src/domain/schemas/fc-bom.ts
  </read_first>
  <behavior>
    - Test: buildCsvRows() with cableSchedule present emits a "Section,Cable Schedule,..." separator row followed by 3 data rows (Server-Leaf, Leaf-Spine, VLT) each with 7 columns and correct SKU values in Notes column
    - Test: buildCsvRows() with cableSchedule undefined emits zero cable schedule rows (no "Cable Schedule" category in output)
    - Test: every cable schedule row array has exactly 7 fields (split by comma, accounting for wrapCsvValue)
    - Test: buildThreeTierCsvRows() with cableSchedule present emits Section separator + 3 data rows (Server-Access, Access-Aggr, Aggr-Core) with correct SKU values
    - Test: buildThreeTierCsvRows() with cableSchedule undefined emits zero cable schedule rows
    - Test: buildFCCsvRows() with islCableLengthSkuM present emits Section separator + 1 ISL cable length row with SKU value
    - Test: buildFCCsvRows() with islCableLengthSkuM undefined emits zero cable schedule rows
  </behavior>
  <action>
    **Clos CSV (exportCsv.ts):**
    Add after the violations loop (before the final `return`), guarded by `if (bom.cableSchedule)`:
    1. Section separator row: `['Section', 'Cable Schedule', '', '', '', '', '']`
    2. Server-Leaf row: `['Cable Schedule', 'Server-Leaf', 'Server-Leaf', String(bom.serverLeafCables), 'each', conn, 'SKU: ' + bom.cableSchedule.serverLeafSkuM + 'm']`
    3. Leaf-Spine row: `['Cable Schedule', 'Leaf-Spine', 'Leaf-Spine', String(bom.leafSpineCables), 'each', conn, 'SKU: ' + bom.cableSchedule.leafSpineSkuM + 'm']`
    4. VLT row: `['Cable Schedule', 'VLT', 'VLT', String(bom.vltCables), 'each', '100G', 'SKU: ' + bom.cableSchedule.vltSkuM + 'm']`

    **Three-Tier CSV (exportThreeTierCsv.ts):**
    Add after the violations loop, guarded by `if (bom.cableSchedule)`:
    1. Section separator: `['Section', 'Cable Schedule', '', '', '', '', '']`
    2. Server-Access row: `['Cable Schedule', 'Server-Access', 'Server-Access', String(bom.serverAccessCables), 'each', conn, 'SKU: ' + bom.cableSchedule.serverAccessSkuM + 'm']`
    3. Access-Aggr row: `['Cable Schedule', 'Access-Aggr', 'Access-Aggr', String(bom.accessAggrCables), 'each', conn, 'SKU: ' + bom.cableSchedule.accessAggregationSkuM + 'm']`
    4. Aggr-Core row: `['Cable Schedule', 'Aggr-Core', 'Aggr-Core', String(bom.aggrCoreCables), 'each', conn, 'SKU: ' + bom.cableSchedule.aggregationCoreSkuM + 'm']`

    **FC CSV (exportFCCsv.ts):**
    Add after the violations loop, guarded by `if (bom.islCableLengthSkuM != null)`:
    1. Section separator: `['Section', 'Cable Schedule', '', '', '', 'FC', '']`
    2. ISL row: `['Cable Schedule', 'ISL Cable', 'FC ISL', String(bom.islCables), 'each', 'FC', 'SKU: ' + bom.islCableLengthSkuM + 'm']`

    CRITICAL: buildFCCsvRows returns string[][] (not string[]). Push raw arrays, not joined strings.
    CRITICAL: buildCsvRows and buildThreeTierCsvRows push to a string[][] `rows` array that is joined at the end with `rows.map(row => row.map(wrapCsvValue).join(','))`. Push raw 7-element arrays.
    CRITICAL: Every row must have exactly 7 elements to preserve Converged CSV compatibility.
  </action>
  <verify>
    <automated>npx vitest run src/features/export/exportCsv.test.ts src/features/export/exportThreeTierCsv.test.ts src/features/export/exportFCCsv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Cable Schedule" src/features/export/exportCsv.ts returns >= 4 (section + 3 data rows)
    - grep -c "Cable Schedule" src/features/export/exportThreeTierCsv.ts returns >= 4
    - grep -c "Cable Schedule" src/features/export/exportFCCsv.ts returns >= 2 (section + 1 ISL row)
    - grep "islCableLengthSkuM" src/features/export/exportFCCsv.ts shows top-level access (no .cableSchedule. prefix)
    - grep "bom.cableSchedule" src/features/export/exportCsv.ts shows null guard before field access
    - All 3 test files pass with cable schedule test cases
  </acceptance_criteria>
  <done>
    CSV exports for all 3 modes emit cable schedule rows when BOM data is present, skip them when absent, and maintain strict 7-column format. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: PDF cable schedule sections for Clos, Three-Tier, and FC</name>
  <files>
    src/features/export/pdf/BOMPage.tsx,
    src/features/export/pdf/ThreeTierBOMPage.tsx,
    src/features/export/pdf/FCBOMPage.tsx
  </files>
  <read_first>
    src/features/export/pdf/BOMPage.tsx,
    src/features/export/pdf/ThreeTierBOMPage.tsx,
    src/features/export/pdf/FCBOMPage.tsx,
    src/domain/schemas/bom.ts,
    src/domain/schemas/three-tier-bom.ts,
    src/domain/schemas/fc-bom.ts
  </read_first>
  <action>
    **Clos PDF (BOMPage.tsx):**
    Add after the Cables table `</View>` (before the closing `</Page>`), guarded by `{bom.cableSchedule && ( ... )}`:
    1. `<Text style={styles.subheading}>Cable Schedule</Text>`
    2. A `<View style={styles.table}>` with a 3-column header row: "Link Type" (colModel), "Qty" (colRole), "SKU" (colQty)
    3. Three data rows using `styles.row`:
       - Server-Leaf: qty = `bom.serverLeafCables`, SKU = `{bom.cableSchedule.serverLeafSkuM}m`
       - Leaf-Spine: qty = `bom.leafSpineCables`, SKU = `{bom.cableSchedule.leafSpineSkuM}m`
       - VLT: qty = `bom.vltCables`, SKU = `{bom.cableSchedule.vltSkuM}m`

    Use existing `styles.colModel` (45%), `styles.colRole` (35%), `styles.colQty` (20%) and `styles.headerText` — no new StyleSheet entries needed. Reuse existing 3-column layout with semantics: col1=Link Type, col2=Qty, col3=SKU.

    **Three-Tier PDF (ThreeTierBOMPage.tsx):**
    Same pattern after Cables table, guarded by `{bom.cableSchedule && ( ... )}`:
    - Server-Access: qty = `bom.serverAccessCables`, SKU = `{bom.cableSchedule.serverAccessSkuM}m`
    - Access-Aggr: qty = `bom.accessAggrCables`, SKU = `{bom.cableSchedule.accessAggregationSkuM}m`
    - Aggr-Core: qty = `bom.aggrCoreCables`, SKU = `{bom.cableSchedule.aggregationCoreSkuM}m`

    **FC PDF (FCBOMPage.tsx):**
    Add after Port Utilization block (before closing `</Page>`), guarded by `{bom.islCableLengthSkuM != null && ( ... )}`:
    - `<Text style={styles.subheading}>Cable Schedule</Text>`
    - Single-row table: ISL Cable, qty = `bom.islCables`, SKU = `{bom.islCableLengthSkuM}m`
    - CRITICAL: Access `bom.islCableLengthSkuM` directly (top-level), NOT `bom.cableSchedule.islCableLengthSkuM`

    DO NOT create new StyleSheet entries. DO NOT modify existing sections. DO NOT touch Converged PDF files (they delegate to these components).
  </action>
  <verify>
    <automated>npx vitest run src/features/export/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep "Cable Schedule" src/features/export/pdf/BOMPage.tsx returns matches
    - grep "Cable Schedule" src/features/export/pdf/ThreeTierBOMPage.tsx returns matches
    - grep "Cable Schedule" src/features/export/pdf/FCBOMPage.tsx returns matches
    - grep "bom.cableSchedule &&" src/features/export/pdf/BOMPage.tsx shows null guard
    - grep "bom.cableSchedule &&" src/features/export/pdf/ThreeTierBOMPage.tsx shows null guard
    - grep "bom.islCableLengthSkuM != null" src/features/export/pdf/FCBOMPage.tsx shows null guard
    - npx tsc --noEmit passes (no type errors from optional field access)
    - No changes to exportConvergedPdf.ts or ConvergedNetStackDocument.tsx
  </acceptance_criteria>
  <done>
    All 3 PDF BOM page components render a Cable Schedule section when data is present, skip it when absent. TypeScript compiles without errors. Converged PDF inherits changes automatically.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/features/export/` -- all export tests pass including new cable schedule cases
2. `npx tsc --noEmit` -- no type errors
3. `npx vitest run` -- full suite green (no regressions)
4. Manual: export a Clos CSV, open in text editor, verify "Cable Schedule" section with 7-column rows
5. Manual: export a PDF, verify Cable Schedule table appears after Cables section
</verification>

<success_criteria>
- CSV exports for Clos, Three-Tier, and FC include cable schedule rows with link type, qty, and SKU when cableSchedule/islCableLengthSkuM is present
- PDF exports for Clos, Three-Tier, and FC include a Cable Schedule table section when data is present
- No cable schedule output when BOM lacks cable schedule data (backward compat)
- All CSV rows maintain 7-column format (Converged export not broken)
- FC uses top-level islCableLengthSkuM (not nested cableSchedule)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-export/28-01-SUMMARY.md`
</output>
