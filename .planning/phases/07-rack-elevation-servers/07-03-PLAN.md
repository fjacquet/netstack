---
phase: 07-rack-elevation-servers
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: [ELEV-01, ELEV-02, ELEV-03]

must_haves:
  truths:
    - "User sees amber server slots in rack elevation"
    - "Changing U-height to 2U/4U/8U visually increases server slot height"
    - "Overflowing a rack shows red border and destructive violation alert"
    - "All 4 locales render translated labels"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for the complete Phase 7 feature set.

Purpose: Confirm that server rendering, U-height selection, and capacity overflow display correctly in the browser before marking the phase complete.
Output: Human-verified sign-off on visual and functional correctness.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-rack-elevation-servers/07-UI-SPEC.md
@.planning/phases/07-rack-elevation-servers/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and type check</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run the complete validation suite to confirm everything is green before visual verification:
    1. `rtk vitest run` — all tests pass
    2. `npx tsc --noEmit` — TypeScript compiles without errors
    3. `npx vite build` — production build succeeds
  </action>
  <verify>
    <automated>rtk vitest run && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - vitest run exits with 0 failures
    - tsc --noEmit exits with code 0
    - vite build exits with code 0
  </acceptance_criteria>
  <done>
    Full test suite, TypeScript, and production build all green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of server rendering and capacity overflow</name>
  <files></files>
  <action>
    Start the dev server and present the verification checklist to the user.
    Run `npx vite dev` to start the development server.
  </action>
  <verify>
    <automated>npx vite build</automated>
  </verify>
  <done>
    Human has verified all visual and functional aspects of Phase 7 and typed "approved".
  </done>
  <what-built>
    Complete Phase 7 feature: servers in rack elevation with configurable U-height and capacity violation alerts.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npx vite dev`
    2. Open http://localhost:5173

    **ELEV-01 -- Server rendering:**
    3. Go to the "Rack Elevation" tab
    4. Verify amber-colored server slots appear above the 3 switch devices (OOB at U1, Leaf B at U2, Leaf A at U3)
    5. Verify server labels show "Server 1", "Server 2", etc.
    6. Verify servers are NOT draggable (click and drag should not work)

    **ELEV-02 -- U-height selection:**
    7. In the input form, find "Server U-Height" Select in the Physical section (below Rack Size)
    8. Change to "2U" -- verify servers in rack elevation are now visually taller (2 slot heights)
    9. Change to "4U" -- verify servers span 4 slot heights
    10. Change to "8U" -- verify servers span 8 slot heights
    11. Change back to "1U" -- verify servers return to single slot height

    **ELEV-03 -- Capacity overflow:**
    12. Set rack count to 1, servers per rack to 40, U-height to 1U, rack size 42U
        -> Expected: 40 + 3 = 43U > 42U -> red rack frame border, destructive violation alert in BOM panel
    13. Reduce servers to 39
        -> Expected: 39 + 3 = 42U = 42U -> no overflow (green/neutral state)
    14. Set U-height to 2U with 20 servers
        -> Expected: 40 + 3 = 43U > 42U -> overflow alert appears
    15. Check BOM panel shows "Rack Capacity Exceeded" alert with rack number, used U, and total U

    **i18n:**
    16. Switch language to French -- verify all new labels are translated (not showing English or key names)
    17. Switch to German and Italian -- same check

    **RackCapacityBadge:**
    18. In the rack selector bar, verify a badge shows "XU / 42U" for the selected rack
    19. When overflowing, verify the badge turns red and shows "overflow"
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All automated checks pass before visual verification.
Human confirms visual and functional correctness.
</verification>

<success_criteria>

- Full test suite, TypeScript, and production build pass
- Human confirms servers render correctly in rack elevation
- Human confirms U-height selection works visually
- Human confirms capacity overflow displays correctly
- Human confirms i18n translations work in all 4 locales
</success_criteria>

<output>
After completion, create `.planning/phases/07-rack-elevation-servers/07-03-SUMMARY.md`
</output>
