---
phase: 18-three-tier-domain-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/catalog/types.ts
  - src/domain/catalog/hardware.ts
  - src/domain/catalog/hardware.test.ts
  - src/domain/schemas/catalog.ts
  - src/domain/schemas/three-tier-input.ts
  - src/domain/schemas/three-tier-bom.ts
autonomous: true
requirements: [TIER-01, TIER-02, TENG-01, TENG-07]

must_haves:
  truths:
    - "Z9264F-ON, Z9332F-ON, Z9432F-ON exist in SWITCH_CATALOG with verified port counts and power specs"
    - "Every switch model in SWITCH_CATALOG has an optional tier field mapping it to valid 3-tier roles"
    - "ThreeTierSizingInputSchema validates topology, independent access/aggregation/core model selectors, and per-tier uplink counts"
    - "ThreeTierBOMSchema defines the output shape with per-tier switch counts, inter-tier cables, and per-boundary oversubscription"
  artifacts:
    - path: "src/domain/catalog/types.ts"
      provides: "SwitchSpec with optional tier and uHeight fields"
      contains: "tier?"
    - path: "src/domain/catalog/hardware.ts"
      provides: "Z-series entries in SWITCH_CATALOG"
      contains: "Z9264F-ON"
    - path: "src/domain/schemas/three-tier-input.ts"
      provides: "ThreeTierSizingInputSchema with accessModel, aggregationModel, coreModel"
      exports: ["ThreeTierSizingInputSchema", "ThreeTierSizingInput"]
    - path: "src/domain/schemas/three-tier-bom.ts"
      provides: "ThreeTierBOMSchema and ThreeTierConstraintViolationSchema"
      exports: ["ThreeTierBOMSchema", "ThreeTierBOM", "ThreeTierConstraintViolationSchema", "ThreeTierConstraintViolation"]
  key_links:
    - from: "src/domain/catalog/hardware.ts"
      to: "src/domain/catalog/types.ts"
      via: "import SwitchSpec"
      pattern: "import.*SwitchSpec.*from.*types"
    - from: "src/domain/schemas/three-tier-bom.ts"
      to: "src/domain/schemas/three-tier-input.ts"
      via: "input field references ThreeTierSizingInputSchema"
      pattern: "ThreeTierSizingInputSchema"
---

<objective>
Add Z-series switches to the hardware catalog, extend the type system with a `tier` field for 3-tier role mapping, and create the Zod schemas for three-tier input and BOM output.

Purpose: Establish the type contracts and catalog data that the three-tier sizing engine (Plan 02) will consume. This is the foundation layer -- no engine logic yet.
Output: Extended SwitchSpec type, 9-model SWITCH_CATALOG, ThreeTierSizingInputSchema, ThreeTierBOMSchema, updated hardware tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-three-tier-domain-engine/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/domain/catalog/types.ts (CURRENT -- will be modified):
```typescript
export interface SwitchSpec {
  modelId: string;
  role: 'leaf' | 'spine' | 'oob';
  downlinkPorts: number;
  downlinkSpeedGbE: number;
  uplinkPorts: number;
  uplinkSpeedGbE?: number;
  additionalUplinkPorts?: number;
  maxPowerW: number;
  typicalPowerW?: number;
  switchingCapacityTbps?: number;
}
```

From src/domain/schemas/catalog.ts (CURRENT -- will be modified):
```typescript
export const SwitchSpecSchema = z.object({
  modelId: z.string().min(1),
  role: z.enum(['leaf', 'spine', 'oob']),
  downlinkPorts: z.number().int().min(0),
  downlinkSpeedGbE: z.number().int().min(0),
  uplinkPorts: z.number().int().min(0),
  uplinkSpeedGbE: z.number().int().optional(),
  additionalUplinkPorts: z.number().int().optional(),
  maxPowerW: z.number().min(0),
  typicalPowerW: z.number().optional(),
  switchingCapacityTbps: z.number().optional(),
});
```

From src/domain/schemas/input.ts (for RackConfigSchema reuse):
```typescript
export const RackConfigSchema = z.object({
  serverCount: z.number().int().min(0).max(500),
});
```

From src/domain/catalog/hardware.ts (CURRENT -- 6 models, will add 3):
```typescript
export const SWITCH_CATALOG = { ... } as const satisfies Record<string, SwitchSpec>;
export type SwitchModelId = keyof typeof SWITCH_CATALOG;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SwitchSpec with tier/uHeight fields and add Z-series catalog entries</name>
  <files>
    src/domain/catalog/types.ts
    src/domain/catalog/hardware.ts
    src/domain/schemas/catalog.ts
    src/domain/catalog/hardware.test.ts
  </files>
  <read_first>
    src/domain/catalog/types.ts
    src/domain/catalog/hardware.ts
    src/domain/schemas/catalog.ts
    src/domain/catalog/hardware.test.ts
  </read_first>
  <action>
1. In `src/domain/catalog/types.ts`, add two optional fields to `SwitchSpec`:
   - `tier?: ('access' | 'aggregation' | 'core')[]` -- valid roles in a 3-tier topology. Undefined = not usable in 3-tier mode.
   - `uHeight?: number` -- rack unit height (default 1U if omitted, 2U for Z9264F-ON)

2. In `src/domain/schemas/catalog.ts`, add matching optional fields to `SwitchSpecSchema`:
   - `tier: z.array(z.enum(['access', 'aggregation', 'core'])).optional()`
   - `uHeight: z.number().int().min(1).optional()`

3. In `src/domain/catalog/hardware.ts`, add `tier` field to ALL existing models:
   - S5248F-ON: `tier: ['access']`
   - S5232F-ON: `tier: ['aggregation']`
   - S5224F-ON: `tier: ['access']`
   - S5212F-ON: `tier: ['access']`
   - S5296F-ON: `tier: ['access']`
   - S3248T-ON: NO tier field (OOB only)

4. In `src/domain/catalog/hardware.ts`, add three new Z-series entries:

   'Z9264F-ON': {
     modelId: 'Z9264F-ON',
     role: 'leaf',
     tier: ['access', 'aggregation'],
     downlinkPorts: 64,
     downlinkSpeedGbE: 100,
     uplinkPorts: 0,
     uplinkSpeedGbE: 100,
     maxPowerW: 750,
     typicalPowerW: 340,
     switchingCapacityTbps: 12.8,
     uHeight: 2,
   }

   'Z9332F-ON': {
     modelId: 'Z9332F-ON',
     role: 'spine',
     tier: ['aggregation', 'core'],
     downlinkPorts: 32,
     downlinkSpeedGbE: 400,
     uplinkPorts: 0,
     uplinkSpeedGbE: 400,
     maxPowerW: 1500,
     typicalPowerW: 900,
     switchingCapacityTbps: 25.6,
     uHeight: 1,
   }

   'Z9432F-ON': {
     modelId: 'Z9432F-ON',
     role: 'spine',
     tier: ['aggregation', 'core'],
     downlinkPorts: 32,
     downlinkSpeedGbE: 400,
     uplinkPorts: 0,
     uplinkSpeedGbE: 400,
     maxPowerW: 1404,
     typicalPowerW: 900,
     switchingCapacityTbps: 25.6,
     uHeight: 1,
   }

5. In `src/domain/catalog/hardware.test.ts`, update the model count assertion from 6 to 9 and add describe blocks for each Z-series model verifying: modelId, role, tier, downlinkPorts, downlinkSpeedGbE, uplinkPorts, maxPowerW, uHeight, switchingCapacityTbps. Also add a test that S3248T-ON has NO tier field.

IMPORTANT: The `tier` field is OPTIONAL (not required). Existing models with no `tier` remain valid. The `role` field is UNCHANGED -- it controls Clos behavior. `tier` only controls 3-tier behavior. This is additive, NOT a replacement.

IMPORTANT: Z-series `downlinkPorts` represents TOTAL configurable ports (symmetric switch). `uplinkPorts: 0` because the uplink/downlink split is logical, not physical -- the engine will compute effective splits from user-specified uplink counts.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/network-sizer && rtk vitest run src/domain/catalog/hardware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Z9264F-ON" src/domain/catalog/hardware.ts returns 1+ (Z-series entry exists)
    - grep -c "Z9332F-ON" src/domain/catalog/hardware.ts returns 1+ (Z-series entry exists)
    - grep -c "Z9432F-ON" src/domain/catalog/hardware.ts returns 1+ (Z-series entry exists)
    - grep -c "tier?" src/domain/catalog/types.ts returns 1 (optional tier field on SwitchSpec)
    - grep -c "uHeight?" src/domain/catalog/types.ts returns 1 (optional uHeight field on SwitchSpec)
    - npx vitest run src/domain/catalog/hardware.test.ts exits 0
    - npx vitest run (full suite) exits 0 with zero regressions
  </acceptance_criteria>
  <done>SWITCH_CATALOG has 9 models (6 existing + 3 Z-series). SwitchSpec has optional `tier` and `uHeight` fields. All existing S-series models have `tier: ['access']` or `tier: ['aggregation']` (except OOB which has none). All existing tests pass. Z-series tests verify specs match Dell documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Create ThreeTierSizingInputSchema and ThreeTierBOMSchema</name>
  <files>
    src/domain/schemas/three-tier-input.ts
    src/domain/schemas/three-tier-bom.ts
  </files>
  <read_first>
    src/domain/schemas/input.ts
    src/domain/schemas/bom.ts
    src/domain/schemas/fc-input.ts
    src/domain/schemas/fc-bom.ts
    src/domain/catalog/hardware.ts
  </read_first>
  <action>
1. Create `src/domain/schemas/three-tier-input.ts`:
   - Import `z` from `'zod'` and `RackConfigSchema` from `'./input'`
   - Define `ThreeTierSizingInputSchema = z.object({...})` with these fields:
     - `racks`: `z.array(RackConfigSchema).min(1).max(200)`
     - `portsPerServerFrontend`: `z.number().int().min(0).max(8).default(1)`
     - `portsPerServerBackend`: `z.number().int().min(0).max(8).default(1)`
     - `connectivityType`: `z.enum(['25G', '100G'])`
     - `cableType`: `z.enum(['DAC', 'AOC', 'fiber'])`
     - `accessModel`: `z.enum(['S5248F-ON', 'S5224F-ON', 'S5212F-ON', 'S5296F-ON', 'Z9264F-ON'])`
     - `activeUplinksPerAccess`: `z.number().int().min(1).max(64).default(4)` -- active uplinks from access to aggregation
     - `aggregationModel`: `z.enum(['Z9264F-ON', 'Z9332F-ON', 'Z9432F-ON', 'S5232F-ON'])`
     - `activeUplinksPerAggregation`: `z.number().int().min(1).max(32).default(4)` -- active uplinks from aggregation to core
     - `coreModel`: `z.enum(['Z9332F-ON', 'Z9432F-ON'])`
     - `borderLeafModel`: `z.enum(['S5248F-ON', 'S5224F-ON', 'S5212F-ON', 'S5296F-ON', 'none'])`
     - `borderLeafCount`: `z.number().int().min(0).max(4)`
     - `rackSize`: `z.enum(['24U', '42U', '50U'])`
     - `serverUHeight`: `z.enum(['1U', '2U', '4U', '8U']).default('1U')`
     - `switchPositioning`: `z.enum(['ToR', 'MoR', 'BoR']).default('ToR')`
   - Export the schema and inferred type: `export type ThreeTierSizingInput = z.infer<typeof ThreeTierSizingInputSchema>;`

2. Create `src/domain/schemas/three-tier-bom.ts`:
   - Import `z` from `'zod'`, `ThreeTierSizingInputSchema` from `'./three-tier-input'`
   - Define `ThreeTierConstraintViolationSchema = z.discriminatedUnion('code', [...])` with violations:
     - `AGGREGATION_CAPACITY_EXCEEDED`: `{ code, accessUplinks: z.number().int(), aggrDownlinks: z.number().int() }`
     - `CORE_CAPACITY_EXCEEDED`: `{ code, aggrUplinks: z.number().int(), coreDownlinks: z.number().int() }`
     - `OOB_PORT_SATURATION`: `{ code, required: z.number().int(), available: z.number().int() }`
     - `DAC_DISTANCE_ADVISORY`: `{ code, rackCount: z.number().int(), cableType: z.literal('DAC') }`
     - `RACK_CAPACITY_EXCEEDED`: `{ code, rackNumber: z.number().int(), usedU: z.number().int(), totalU: z.number().int() }`
   - Define `ThreeTierBOMSchema = z.object({...})` with fields:
     - `racks`, `networkRacks`: `z.number().int().min(0)`
     - `accessSwitches`, `aggregationSwitches`, `coreSwitches`, `oobSwitches`, `borderLeafSwitches`: `z.number().int().min(0)`
     - `serverAccessCables`, `accessAggrCables`, `aggrCoreCables`, `serverOobCables`, `vltCables`: `z.number().int().min(0)`
     - `sfp28Count`, `qsfp28Count`, `qsfp56ddCount`: `z.number().int().min(0)` -- qsfp56ddCount is NEW for 400G inter-tier links
     - `accessToAggrOversubscription`, `aggrToCoreOversubscription`: `z.number().min(0)` -- TENG-05
     - `switchPositioning`: `z.enum(['ToR', 'MoR', 'BoR'])`
     - `recommendedCableLengthM`: `z.number().int().min(0)`
     - `violations`: `z.array(ThreeTierConstraintViolationSchema)`
     - `input`: `ThreeTierSizingInputSchema`
   - Export all schemas, types, and the constraint violation type

Follow the exact same file structure and JSDoc style as `src/domain/schemas/bom.ts` and `src/domain/schemas/fc-bom.ts`.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/network-sizer && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ThreeTierSizingInputSchema" src/domain/schemas/three-tier-input.ts returns 1+ (schema defined)
    - grep -c "accessModel" src/domain/schemas/three-tier-input.ts returns 1+ (TENG-07: independent model selector)
    - grep -c "aggregationModel" src/domain/schemas/three-tier-input.ts returns 1+ (TENG-07: independent model selector)
    - grep -c "coreModel" src/domain/schemas/three-tier-input.ts returns 1+ (TENG-07: independent model selector)
    - grep -c "ThreeTierBOMSchema" src/domain/schemas/three-tier-bom.ts returns 1+ (BOM schema defined)
    - grep -c "accessToAggrOversubscription" src/domain/schemas/three-tier-bom.ts returns 1+ (TENG-05: per-boundary oversub)
    - grep -c "aggrToCoreOversubscription" src/domain/schemas/three-tier-bom.ts returns 1+ (TENG-05: per-boundary oversub)
    - grep -c "AGGREGATION_CAPACITY_EXCEEDED" src/domain/schemas/three-tier-bom.ts returns 1+ (violation type)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ThreeTierSizingInputSchema exists with accessModel/aggregationModel/coreModel selectors (TENG-07), per-tier uplink counts, and all shared fields. ThreeTierBOMSchema exists with per-tier switch counts, inter-tier cable counts (TENG-06 shape), per-boundary oversubscription (TENG-05 shape), and typed constraint violations. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/domain/catalog/hardware.test.ts` -- all catalog tests pass including new Z-series assertions
- `npx tsc --noEmit` -- zero TypeScript errors
- `npx vitest run` -- full suite green, zero regressions from existing 416 tests
</verification>

<success_criteria>
- SWITCH_CATALOG contains exactly 9 models (6 S-series + 3 Z-series)
- SwitchSpec interface has optional `tier` and `uHeight` fields
- SwitchSpecSchema (Zod) has matching optional `tier` and `uHeight` fields
- ThreeTierSizingInputSchema defines independent model selectors for all 3 tiers
- ThreeTierBOMSchema defines per-tier counts, inter-tier cables, and dual oversubscription ratios
- All 416+ existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-three-tier-domain-engine/18-01-SUMMARY.md`
</output>
