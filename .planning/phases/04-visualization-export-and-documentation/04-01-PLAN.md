---
phase: 04-visualization-export-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/popover.tsx
  - src/components/ui/scroll-area.tsx
  - src/features/topology/types.ts
  - src/features/topology/utils/saturation.ts
  - src/features/topology/utils/saturation.test.ts
  - src/features/rack-elevation/types.ts
  - src/features/export/types.ts
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/it/translation.json
autonomous: true
requirements: [VIZ-03]

must_haves:
  truths:
    - "@xyflow/react, @react-pdf/renderer, and html-to-image are installed and importable"
    - "shadcn popover and scroll-area components exist and are importable"
    - "Topology, rack elevation, and export type contracts are defined and exported"
    - "Saturation color helper returns correct Tailwind classes for healthy, warning, and saturated thresholds"
    - "All Phase 4 i18n keys exist in all 4 locale files"
  artifacts:
    - path: "src/features/topology/types.ts"
      provides: "SwitchNodeData, TopologyGraphResult type contracts"
      exports: ["SwitchNodeData", "TopologyGraphResult"]
    - path: "src/features/topology/utils/saturation.ts"
      provides: "getSaturationBorderClass and getSaturationLevel helpers"
      exports: ["getSaturationBorderClass", "getSaturationLevel"]
    - path: "src/features/rack-elevation/types.ts"
      provides: "RackDevice type contract"
      exports: ["RackDevice"]
    - path: "src/features/export/types.ts"
      provides: "CsvRow type contract"
      exports: ["CsvRow"]
    - path: "src/components/ui/popover.tsx"
      provides: "shadcn Popover component"
    - path: "src/components/ui/scroll-area.tsx"
      provides: "shadcn ScrollArea component"
  key_links:
    - from: "src/features/topology/utils/saturation.ts"
      to: "src/features/sizing/BOMPanel.tsx"
      via: "Same threshold logic (80%, 100%) reused from BOMPanel getProgressColor"
      pattern: "pct < 80.*pct < 100"
---

<objective>
Install Phase 4 dependencies (@xyflow/react, @react-pdf/renderer, html-to-image), add shadcn popover and scroll-area components, define type contracts for topology/rack/export features, create shared saturation color helper with tests, and populate all Phase 4 i18n keys across 4 locales.

Purpose: Foundation layer that all Phase 4 feature plans depend on. Establishes contracts so Plans 02-05 can execute in parallel without ambiguity about shared types.
Output: Installed packages, shadcn components, type files, saturation utility with tests, and i18n keys in all 4 locales.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visualization-export-and-documentation/04-CONTEXT.md
@.planning/phases/04-visualization-export-and-documentation/04-RESEARCH.md
@.planning/phases/04-visualization-export-and-documentation/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/domain/schemas/bom.ts:

```typescript
export type ConstraintViolation = z.infer<typeof ConstraintViolationSchema>;
// codes: 'OOB_PORT_SATURATION' | 'SPINE_CAPACITY_EXCEEDED' | 'DAC_DISTANCE_ADVISORY'

export type NetworkBOM = z.infer<typeof NetworkBOMSchema>;
// fields: racks, leafSwitches, spineSwitches, oobSwitches,
//   leafSpineCables, serverLeafCables, serverOobCables,
//   sfp28Count, qsfp28Count, vltCables,
//   oversubscriptionRatio, violations, input
```

From src/domain/schemas/input.ts:

```typescript
export type SizingInput = z.infer<typeof SizingInputSchema>;
// fields: totalServers, serversPerRack, connectivityType, cableType, leafModel
```

From src/domain/catalog/hardware.ts:

```typescript
export const SWITCH_CATALOG: Record<string, SwitchSpec>;
// keys: 'S5248F-ON', 'S5232F-ON', 'S5224F-ON', 'S5212F-ON', 'S3248T-ON'
// SwitchSpec fields: modelId, role, downlinkPorts, downlinkSpeedGbE, uplinkPorts, maxPowerW
```

From src/features/sizing/BOMPanel.tsx:

```typescript
function getProgressColor(pct: number): string {
  if (pct < 80) return 'bg-[hsl(142_76%_36%)] dark:bg-[hsl(142_69%_58%)]'
  if (pct < 100) return 'bg-[hsl(38_92%_50%)] dark:bg-[hsl(38_95%_64%)]'
  return 'bg-destructive'
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and shadcn components, define type contracts</name>
  <files>
    package.json, package-lock.json,
    src/components/ui/popover.tsx, src/components/ui/scroll-area.tsx,
    src/features/topology/types.ts, src/features/topology/index.ts,
    src/features/rack-elevation/types.ts, src/features/rack-elevation/index.ts,
    src/features/export/types.ts, src/features/export/index.ts
  </files>
  <read_first>
    package.json (current dependencies),
    src/components/ui/tooltip.tsx (shadcn component pattern reference),
    src/domain/schemas/bom.ts (NetworkBOM type for downstream contracts),
    src/domain/schemas/input.ts (SizingInput type),
    src/domain/catalog/hardware.ts (SwitchSpec, SWITCH_CATALOG)
  </read_first>
  <action>
    **Step 1: Install npm packages**
    Run: `npm install @xyflow/react @react-pdf/renderer html-to-image`

    **Step 2: Install shadcn components**
    Use the `node -e execSync` workaround pattern established in Phase 3 (npx shadcn fails in non-interactive mode):
    ```
    node -e "require('child_process').execSync('npx shadcn@latest add popover -y', { cwd: '/Users/fjacquet/Projects/network-sizer', stdio: 'inherit' })"
    node -e "require('child_process').execSync('npx shadcn@latest add scroll-area -y', { cwd: '/Users/fjacquet/Projects/network-sizer', stdio: 'inherit' })"
    ```

    **Step 3: Create topology type contracts**
    Create `src/features/topology/types.ts` with:
    ```typescript
    import type { Node, Edge } from '@xyflow/react'

    export type SwitchNodeData = {
      model: string
      role: 'spine' | 'leaf' | 'oob'
      usedPorts: number
      totalPorts: number
      rackIndex?: number
    }

    export type RackNodeData = {
      rackIndex: number
      serverCount: number
    }

    export type TopologyGraphResult = {
      nodes: Node<SwitchNodeData | RackNodeData>[]
      edges: Edge[]
    }
    ```
    Create `src/features/topology/index.ts` as empty barrel export (will be populated in Plan 02).

    **Step 4: Create rack elevation type contracts**
    Create `src/features/rack-elevation/types.ts` with:
    ```typescript
    export type RackDevice = {
      id: string
      model: string
      role: 'leaf' | 'spine' | 'oob'
      label: string
      uSlot: number
      usedPorts: number
      totalPorts: number
    }
    ```
    Create `src/features/rack-elevation/index.ts` as empty barrel export.

    **Step 5: Create export type contracts**
    Create `src/features/export/types.ts` with:
    ```typescript
    export type CsvRow = {
      category: 'Switch' | 'Cable' | 'Transceiver'
      modelOrType: string
      role: string
      quantity: number
      unit: string
      connectivity: string
      notes: string
    }
    ```
    Create `src/features/export/index.ts` as empty barrel export.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@xyflow/react"`, `"@react-pdf/renderer"`, and `"html-to-image"` in dependencies
    - src/components/ui/popover.tsx exists and exports `Popover`, `PopoverTrigger`, `PopoverContent`
    - src/components/ui/scroll-area.tsx exists and exports `ScrollArea`, `ScrollBar`
    - src/features/topology/types.ts exports `SwitchNodeData`, `RackNodeData`, `TopologyGraphResult`
    - src/features/rack-elevation/types.ts exports `RackDevice`
    - src/features/export/types.ts exports `CsvRow`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All packages installed, shadcn components added, type contracts defined and TypeScript compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create saturation color helper and populate Phase 4 i18n keys</name>
  <files>
    src/features/topology/utils/saturation.ts,
    src/features/topology/utils/saturation.test.ts,
    src/i18n/locales/en/translation.json,
    src/i18n/locales/fr/translation.json,
    src/i18n/locales/de/translation.json,
    src/i18n/locales/it/translation.json
  </files>
  <read_first>
    src/features/sizing/BOMPanel.tsx (getProgressColor pattern to replicate for border colors),
    src/i18n/locales/en/translation.json (existing key structure),
    src/i18n/locales/fr/translation.json (existing FR translations),
    src/i18n/locales/de/translation.json (existing DE translations),
    src/i18n/locales/it/translation.json (existing IT translations),
    .planning/phases/04-visualization-export-and-documentation/04-UI-SPEC.md (copywriting contract and color values)
  </read_first>
  <behavior>
    - getSaturationLevel(80, 100) returns 'saturated' (pct >= 100%)
    - getSaturationLevel(79, 100) returns 'warning' (80% <= pct < 100%)
    - getSaturationLevel(50, 100) returns 'healthy' (pct < 80%)
    - getSaturationBorderClass(50, 100) returns green border class containing 'hsl(142'
    - getSaturationBorderClass(85, 100) returns amber border class containing 'hsl(38'
    - getSaturationBorderClass(100, 100) returns destructive border class containing 'destructive'
    - getSaturationBorderClass(0, 0) handles edge case (0/0 = healthy, not NaN crash)
  </behavior>
  <action>
    **Step 1: Create test file** `src/features/topology/utils/saturation.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { getSaturationLevel, getSaturationBorderClass } from './saturation'

    describe('getSaturationLevel', () => {
      it('returns healthy when pct < 80%', () => {
        expect(getSaturationLevel(10, 48)).toBe('healthy')
      })
      it('returns warning when pct >= 80% and < 100%', () => {
        expect(getSaturationLevel(40, 48)).toBe('warning')  // 83%
      })
      it('returns saturated when pct >= 100%', () => {
        expect(getSaturationLevel(48, 48)).toBe('saturated')
        expect(getSaturationLevel(50, 48)).toBe('saturated')
      })
      it('handles zero totalPorts without crashing', () => {
        expect(getSaturationLevel(0, 0)).toBe('healthy')
      })
    })

    describe('getSaturationBorderClass', () => {
      it('returns green border for healthy', () => {
        expect(getSaturationBorderClass(10, 48)).toContain('hsl(142')
      })
      it('returns amber border for warning', () => {
        expect(getSaturationBorderClass(40, 48)).toContain('hsl(38')
      })
      it('returns destructive border for saturated', () => {
        expect(getSaturationBorderClass(48, 48)).toContain('destructive')
      })
    })
    ```

    Run test — must FAIL (RED).

    **Step 2: Create** `src/features/topology/utils/saturation.ts`:
    ```typescript
    export type SaturationLevel = 'healthy' | 'warning' | 'saturated'

    export function getSaturationLevel(usedPorts: number, totalPorts: number): SaturationLevel {
      if (totalPorts === 0) return 'healthy'
      const pct = (usedPorts / totalPorts) * 100
      if (pct >= 100) return 'saturated'
      if (pct >= 80) return 'warning'
      return 'healthy'
    }

    export function getSaturationBorderClass(usedPorts: number, totalPorts: number): string {
      const level = getSaturationLevel(usedPorts, totalPorts)
      switch (level) {
        case 'healthy':
          return 'border-[hsl(142_76%_36%)] dark:border-[hsl(142_69%_58%)]'
        case 'warning':
          return 'border-[hsl(38_92%_50%)] dark:border-[hsl(38_95%_64%)]'
        case 'saturated':
          return 'border-destructive'
      }
    }
    ```

    Run test — must PASS (GREEN).

    **Step 3: Add Phase 4 i18n keys** to all 4 locale files. Merge these keys into the existing JSON (do NOT overwrite existing keys). Add three new top-level namespaces: `topology`, `rack`, `export`.

    **EN keys** (canonical — from UI-SPEC copywriting contract):
    ```json
    {
      "topology": {
        "heading": "Topology Diagram",
        "fitView": "Fit View",
        "resetLayout": "Reset Layout",
        "legend": "Legend",
        "emptyHeading": "No topology available",
        "emptyBody": "Enter sizing parameters in the Sizing tab to generate the topology diagram.",
        "legendLeaf": "Leaf Switch",
        "legendSpine": "Spine Switch",
        "legendOob": "OOB Switch",
        "legendRack": "Rack",
        "legendHealthy": "Healthy (< 80%)",
        "legendWarning": "Warning (\u2265 80%)",
        "legendCritical": "Saturated (\u2265 100%)",
        "legendVlt": "VLT Link",
        "nodeAriaLabel": "{{model}} {{role}} \u2014 click for port details",
        "portUtilization": "Port Utilization",
        "portsFormat": "{{used}} / {{available}} used ({{pct}}%)"
      },
      "rack": {
        "heading": "Rack Elevation",
        "selectorLabel": "Rack",
        "selectorOptionFormat": "Rack {{n}} \u2014 {{switchCount}} switches",
        "emptyHeading": "No racks to display",
        "emptyBody": "Enter sizing parameters in the Sizing tab to generate the rack elevation view."
      },
      "export": {
        "heading": "Export",
        "csvHeading": "CSV Export",
        "csvDescription": "Download the Bill of Materials as a CSV file. Opens in Excel and Google Sheets.",
        "csvButton": "Export CSV",
        "pdfHeading": "PDF Report",
        "pdfDescription": "Generate a formatted report including BOM summary, sizing inputs, and topology diagram.",
        "pdfButton": "Export PDF",
        "printHeading": "Print",
        "printDescription": "Print the current view. Navigation controls are hidden automatically.",
        "printButton": "Print",
        "disabledTooltip": "Calculate a BOM first",
        "pdfGenerating": "Generating PDF\u2026",
        "pdfErrorHeading": "PDF generation failed",
        "pdfErrorBody": "Unable to generate the report. Check your sizing inputs and try again."
      }
    }
    ```

    **FR keys** (French translations):
    ```json
    {
      "topology": {
        "heading": "Diagramme de topologie",
        "fitView": "Ajuster la vue",
        "resetLayout": "R\u00e9initialiser",
        "legend": "L\u00e9gende",
        "emptyHeading": "Aucune topologie disponible",
        "emptyBody": "Saisissez les param\u00e8tres de dimensionnement dans l\u2019onglet Dimensionnement pour g\u00e9n\u00e9rer le diagramme.",
        "legendLeaf": "Switch Leaf",
        "legendSpine": "Switch Spine",
        "legendOob": "Switch OOB",
        "legendRack": "Baie",
        "legendHealthy": "Normal (< 80\u00a0%)",
        "legendWarning": "Attention (\u2265 80\u00a0%)",
        "legendCritical": "Satur\u00e9 (\u2265 100\u00a0%)",
        "legendVlt": "Lien VLT",
        "nodeAriaLabel": "{{model}} {{role}} \u2014 cliquez pour les d\u00e9tails des ports",
        "portUtilization": "Utilisation des ports",
        "portsFormat": "{{used}} / {{available}} utilis\u00e9s ({{pct}}\u00a0%)"
      },
      "rack": {
        "heading": "\u00c9l\u00e9vation de baie",
        "selectorLabel": "Baie",
        "selectorOptionFormat": "Baie {{n}} \u2014 {{switchCount}} switches",
        "emptyHeading": "Aucune baie \u00e0 afficher",
        "emptyBody": "Saisissez les param\u00e8tres de dimensionnement dans l\u2019onglet Dimensionnement pour g\u00e9n\u00e9rer la vue."
      },
      "export": {
        "heading": "Exporter",
        "csvHeading": "Export CSV",
        "csvDescription": "T\u00e9l\u00e9chargez la nomenclature au format CSV. Compatible Excel et Google Sheets.",
        "csvButton": "Exporter CSV",
        "pdfHeading": "Rapport PDF",
        "pdfDescription": "G\u00e9n\u00e9rez un rapport format\u00e9 avec la nomenclature, les param\u00e8tres et le diagramme de topologie.",
        "pdfButton": "Exporter PDF",
        "printHeading": "Imprimer",
        "printDescription": "Imprimez la vue courante. Les contr\u00f4les de navigation sont masqu\u00e9s automatiquement.",
        "printButton": "Imprimer",
        "disabledTooltip": "Calculez d\u2019abord une nomenclature",
        "pdfGenerating": "G\u00e9n\u00e9ration du PDF\u2026",
        "pdfErrorHeading": "\u00c9chec de la g\u00e9n\u00e9ration PDF",
        "pdfErrorBody": "Impossible de g\u00e9n\u00e9rer le rapport. V\u00e9rifiez vos param\u00e8tres et r\u00e9essayez."
      }
    }
    ```

    **DE keys** (German translations):
    ```json
    {
      "topology": {
        "heading": "Topologiediagramm",
        "fitView": "Ansicht anpassen",
        "resetLayout": "Layout zur\u00fccksetzen",
        "legend": "Legende",
        "emptyHeading": "Keine Topologie verf\u00fcgbar",
        "emptyBody": "Geben Sie Dimensionierungsparameter im Reiter Dimensionierung ein, um das Diagramm zu erstellen.",
        "legendLeaf": "Leaf-Switch",
        "legendSpine": "Spine-Switch",
        "legendOob": "OOB-Switch",
        "legendRack": "Rack",
        "legendHealthy": "Normal (< 80\u00a0%)",
        "legendWarning": "Warnung (\u2265 80\u00a0%)",
        "legendCritical": "Ges\u00e4ttigt (\u2265 100\u00a0%)",
        "legendVlt": "VLT-Verbindung",
        "nodeAriaLabel": "{{model}} {{role}} \u2014 klicken f\u00fcr Portdetails",
        "portUtilization": "Portauslastung",
        "portsFormat": "{{used}} / {{available}} belegt ({{pct}}\u00a0%)"
      },
      "rack": {
        "heading": "Rack-Ansicht",
        "selectorLabel": "Rack",
        "selectorOptionFormat": "Rack {{n}} \u2014 {{switchCount}} Switches",
        "emptyHeading": "Keine Racks vorhanden",
        "emptyBody": "Geben Sie Dimensionierungsparameter im Reiter Dimensionierung ein, um die Rack-Ansicht zu erstellen."
      },
      "export": {
        "heading": "Exportieren",
        "csvHeading": "CSV-Export",
        "csvDescription": "Laden Sie die St\u00fcckliste als CSV-Datei herunter. Kompatibel mit Excel und Google Sheets.",
        "csvButton": "CSV exportieren",
        "pdfHeading": "PDF-Bericht",
        "pdfDescription": "Erstellen Sie einen formatierten Bericht mit St\u00fcckliste, Parametern und Topologiediagramm.",
        "pdfButton": "PDF exportieren",
        "printHeading": "Drucken",
        "printDescription": "Aktuelle Ansicht drucken. Navigationsleisten werden automatisch ausgeblendet.",
        "printButton": "Drucken",
        "disabledTooltip": "Berechnen Sie zuerst eine St\u00fcckliste",
        "pdfGenerating": "PDF wird erstellt\u2026",
        "pdfErrorHeading": "PDF-Erstellung fehlgeschlagen",
        "pdfErrorBody": "Der Bericht konnte nicht erstellt werden. \u00dcberpr\u00fcfen Sie Ihre Parameter und versuchen Sie es erneut."
      }
    }
    ```

    **IT keys** (Italian translations):
    ```json
    {
      "topology": {
        "heading": "Diagramma di topologia",
        "fitView": "Adatta vista",
        "resetLayout": "Ripristina layout",
        "legend": "Legenda",
        "emptyHeading": "Nessuna topologia disponibile",
        "emptyBody": "Inserisci i parametri di dimensionamento nella scheda Dimensionamento per generare il diagramma.",
        "legendLeaf": "Switch Leaf",
        "legendSpine": "Switch Spine",
        "legendOob": "Switch OOB",
        "legendRack": "Rack",
        "legendHealthy": "Normale (< 80%)",
        "legendWarning": "Attenzione (\u2265 80%)",
        "legendCritical": "Saturo (\u2265 100%)",
        "legendVlt": "Collegamento VLT",
        "nodeAriaLabel": "{{model}} {{role}} \u2014 clicca per i dettagli delle porte",
        "portUtilization": "Utilizzo porte",
        "portsFormat": "{{used}} / {{available}} utilizzate ({{pct}}%)"
      },
      "rack": {
        "heading": "Vista rack",
        "selectorLabel": "Rack",
        "selectorOptionFormat": "Rack {{n}} \u2014 {{switchCount}} switch",
        "emptyHeading": "Nessun rack da visualizzare",
        "emptyBody": "Inserisci i parametri di dimensionamento nella scheda Dimensionamento per generare la vista rack."
      },
      "export": {
        "heading": "Esporta",
        "csvHeading": "Esportazione CSV",
        "csvDescription": "Scarica la distinta materiali come file CSV. Compatibile con Excel e Google Sheets.",
        "csvButton": "Esporta CSV",
        "pdfHeading": "Report PDF",
        "pdfDescription": "Genera un report formattato con distinta materiali, parametri e diagramma di topologia.",
        "pdfButton": "Esporta PDF",
        "printHeading": "Stampa",
        "printDescription": "Stampa la vista corrente. I controlli di navigazione vengono nascosti automaticamente.",
        "printButton": "Stampa",
        "disabledTooltip": "Calcola prima una distinta materiali",
        "pdfGenerating": "Generazione PDF in corso\u2026",
        "pdfErrorHeading": "Generazione PDF non riuscita",
        "pdfErrorBody": "Impossibile generare il report. Verifica i parametri e riprova."
      }
    }
    ```
  </action>
  <verify>
    <automated>rtk npx vitest run src/features/topology/utils/saturation.test.ts && rtk npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/features/topology/utils/saturation.ts exports `getSaturationLevel` and `getSaturationBorderClass`
    - src/features/topology/utils/saturation.test.ts has 7+ test cases and all pass
    - src/i18n/locales/en/translation.json contains keys `topology.heading`, `rack.heading`, `export.heading`
    - src/i18n/locales/fr/translation.json contains keys `topology.heading`, `rack.heading`, `export.heading`
    - src/i18n/locales/de/translation.json contains keys `topology.heading`, `rack.heading`, `export.heading`
    - src/i18n/locales/it/translation.json contains keys `topology.heading`, `rack.heading`, `export.heading`
    - `npx vitest run` full suite passes with no regressions
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Saturation helper tests pass, all 4 locale files have complete Phase 4 i18n keys, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (type contracts valid)
- `npx vitest run` full suite green (saturation tests + all existing tests pass)
- `@xyflow/react`, `@react-pdf/renderer`, `html-to-image` in package.json dependencies
- All 4 locale files contain `topology.*`, `rack.*`, `export.*` namespaces
</verification>

<success_criteria>
All Phase 4 dependencies installed, type contracts defined, saturation utility tested, and i18n keys populated across all 4 locales. Plan 02-05 can proceed without ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visualization-export-and-documentation/04-01-SUMMARY.md`
</output>
