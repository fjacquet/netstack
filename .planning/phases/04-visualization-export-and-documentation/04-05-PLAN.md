---
phase: 04-visualization-export-and-documentation
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/adr/0005-xyflow-topology-diagram.md
  - docs/adr/0006-react-pdf-lazy-loading.md
  - docs/adr/0007-vlt-cable-modeling.md
  - docs/adr/0008-i18n-react-i18next.md
  - docs/prd.md
  - docs/userguide.md
  - docs/CHANGELOG.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "ADRs cover key architecture decisions made in Phases 2-4"
    - "PRD (docs/prd.md) formalizes all 28 v1 requirements with acceptance criteria"
    - "User Guide (docs/userguide.md) explains how to size a deployment, interpret the BOM, and export results"
    - "Changelog records v1.0 as the first entry with all delivered features"
  artifacts:
    - path: "docs/adr/0005-xyflow-topology-diagram.md"
      provides: "ADR for topology library choice"
      contains: "@xyflow/react"
    - path: "docs/prd.md"
      provides: "Product Requirements Document"
      contains: "SIZE-01"
    - path: "docs/userguide.md"
      provides: "End-user guide"
      contains: "Sizing"
    - path: "docs/CHANGELOG.md"
      provides: "Version history"
      contains: "1.0.0"
  key_links:
    - from: "docs/prd.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Formalizes all 28 v1 requirement IDs"
      pattern: "SIZE-01|BOM-01|VIZ-01|EXP-01|DOC-01"
---

<objective>
Update existing docs (prd.md, userguide.md) with Phase 2-4 features, add new ADRs (0005-0008) for key decisions, and create CHANGELOG.md. Extends existing docs/adr/ structure.

Purpose: DOC-01/DOC-02/DOC-03/DOC-04 — complete the v1.0 milestone documentation.
Output: 4 new ADR files, updated prd.md and userguide.md, new CHANGELOG.md.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-visualization-export-and-documentation/04-CONTEXT.md

<interfaces>
<!-- Key source files that documentation must accurately describe -->

From CLAUDE.md - Architecture:
```
Domain (pure TS, no React) -> Store (Zustand) -> Features (React components)
```

From CLAUDE.md - Sizing Formulas:
```
Racks: ceil(totalServers / serversPerRack)
Leafs: 2 x racks (redundant ToR pair)
Spines: max(4, ceil(leafSwitches / 32))
OOB: racks x ceil((serversPerRack + 2) / 48)
```

From REQUIREMENTS.md:
28 v1 requirements: SIZE-01..07, BOM-01..04, CAT-01..03, VIZ-01..03, EXP-01..03, UX-01..04, DOC-01..04
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new ADRs and update PRD</name>
  <files>docs/adr/0005-xyflow-topology-diagram.md, docs/adr/0006-react-pdf-lazy-loading.md, docs/adr/0007-vlt-cable-modeling.md, docs/adr/0008-i18n-react-i18next.md, docs/prd.md</files>
  <read_first>
    docs/adr/0001-leaf-spine-architecture.md (existing ADR format to follow),
    docs/prd.md (existing PRD to update),
    CLAUDE.md (architecture, conventions),
    .planning/REQUIREMENTS.md (28 v1 requirements with IDs),
    .planning/ROADMAP.md (4-phase delivery plan)
  </read_first>
  <action>
    **New ADR files in docs/adr/ — follow existing format (Status/Context/Decision/Consequences)**

    Create 4 new ADR files continuing the numbering:

    - `docs/adr/0005-xyflow-topology-diagram.md` — Decision to use @xyflow/react (not deprecated reactflow) for interactive topology diagrams with deterministic layout
    - `docs/adr/0006-react-pdf-lazy-loading.md` — Decision to use @react-pdf/renderer with dynamic import() for PDF generation (not usePDF hook, not PDFDownloadLink)
    - `docs/adr/0007-vlt-cable-modeling.md` — Decision to model VLT interconnect cables (2 per leaf pair) and split transceivers by type (SFP28 for 25G, QSFP28 for 100G)
    - `docs/adr/0008-i18n-react-i18next.md` — Decision to use react-i18next with synchronous JSON imports (not HTTP backend) for 4-locale support (EN/FR/DE/IT)

    **Update docs/prd.md — extend with all 28 v1 requirements**

    Read existing prd.md, then update to include:
    - Complete requirements table with all 28 IDs (SIZE-01 through DOC-04)
    - Acceptance criteria for each requirement
    - Phase traceability (which phase delivered each requirement)
    - Out of scope section
    - v2 roadmap items from REQUIREMENTS.md
  </action>
  <verify>
    <automated>test -f docs/adr/0005-xyflow-topology-diagram.md && test -f docs/adr/0008-i18n-react-i18next.md && grep -q "SIZE-01" docs/prd.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/0005-xyflow-topology-diagram.md exists and contains "Status", "Context", "Decision"
    - docs/adr/0006-react-pdf-lazy-loading.md exists and mentions "dynamic import"
    - docs/adr/0007-vlt-cable-modeling.md exists and mentions "VLT" and "SFP28"
    - docs/adr/0008-i18n-react-i18next.md exists and mentions "synchronous"
    - docs/prd.md contains all 28 requirement IDs: SIZE-01 through DOC-04
    - docs/prd.md has acceptance criteria for requirements
  </acceptance_criteria>
  <done>4 new ADRs document key Phase 2-4 decisions. PRD updated with all 28 v1 requirements and acceptance criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Update User Guide and create Changelog</name>
  <files>docs/userguide.md, docs/CHANGELOG.md</files>
  <read_first>
    CLAUDE.md (project description, build commands, hardware catalog),
    .planning/REQUIREMENTS.md (feature list for changelog),
    .planning/ROADMAP.md (phase descriptions and completion dates),
    src/i18n/locales/en/translation.json (UI copy for user guide screenshots/descriptions)
  </read_first>
  <action>
    **docs/userguide.md — Update existing User Guide**

    Read the existing userguide.md first. Then update/extend it with Phase 2-4 features.
    Written for end-users (network engineers, procurement). No code. Describe the UI.

    Ensure it covers:
    1. **Introduction** — What NetStack does: calculate Dell Leaf-Spine BOM from server count.
    2. **Getting Started** — Open the app URL (GitHub Pages). No installation needed. Works in any modern browser.
    3. **Sizing a Deployment** —
       - Enter Total Servers (1-10,000)
       - Enter Servers per Rack (1-48, OOB alert above 46)
       - Select Connectivity Type (25G or 100G)
       - Select Cable Type (DAC, AOC, or Fiber)
       - Select Leaf Switch Model (S5248F-ON, S5224F-ON, or S5212F-ON)
       - Results update automatically as you type
    4. **Reading the Bill of Materials** —
       - Oversubscription ratio: green (optimal, <=3:1), amber (acceptable, <=6:1), red (critical, >6:1)
       - Switches table: model, role, quantity, port utilization bar
       - Cables table: type, category, quantity
       - Alerts: OOB saturation, spine capacity, DAC distance advisory
    5. **Topology Diagram** —
       - 3-tier layout: spines (top), leafs (middle), racks (bottom)
       - Node colors: blue=leaf, purple=spine, gray=OOB
       - Border glow: green (healthy), amber (>80%), red (saturated)
       - Controls: Fit View, Reset Layout, Legend toggle
       - Click any node for port details
    6. **Rack Elevation** —
       - Select a rack from the dropdown
       - View device placement: OOB at U1, Leaf B at U2, Leaf A at U3
       - Drag devices to rearrange (not persisted)
    7. **Exporting Results** —
       - CSV: downloads immediately, opens in Excel/Google Sheets
       - PDF: generates a multi-page report (takes a moment on first use)
       - Print: Ctrl+P prints the current view cleanly
    8. **Settings** —
       - Theme: light/dark mode toggle (sun/moon icon)
       - Language: EN, FR, DE, IT via dropdown
    9. **Hardware Reference** — Table of 5 Dell switch models (from CLAUDE.md)
    10. **Troubleshooting** —
        - "BOM shows no results" — enter at least 1 server
        - "PDF is slow" — first generation loads the PDF library, subsequent are faster
        - "Topology looks wrong" — click Reset Layout

    **docs/CHANGELOG.md — Changelog**

    Keep it simple. Follow keepachangelog.com format.

    ```markdown
    # Changelog

    All notable changes to NetStack are documented in this file.

    ## [1.0.0] - 2026-03-17

    ### Added
    - Pure sizing engine: `calculateBOM(SizingInput) -> NetworkBOM` with rack, leaf, spine, OOB, and cable calculations
    - Hardware catalog: 5 Dell PowerSwitch models (S5248F-ON, S5232F-ON, S5224F-ON, S5212F-ON, S3248T-ON)
    - JSON override for custom switch models at runtime
    - Constraint violations: OOB port saturation, spine capacity exceeded, DAC distance advisory
    - Input form with live recalculation (no submit button)
    - Bill of Materials panel with oversubscription badge, switch/cable tables, port utilization bars
    - Interactive topology diagram (@xyflow/react) with deterministic 3-tier layout
    - Custom node types with role colors and saturation border glow
    - Rack elevation view with U-slot numbering and drag-to-reorder
    - CSV export with UTF-8 BOM for Excel compatibility
    - PDF report with BOM summary, sizing inputs, and topology diagram
    - Print stylesheet (Ctrl+P) with clean layout
    - Light/dark mode with system preference detection
    - Internationalization: EN, FR, DE, IT
    - Responsive layout for tablet (768px+) and desktop (1280px+)
    - GitHub Pages deployment via GitHub Actions
    - Architecture Reference Document (ARD)
    - Product Requirements Document (PRD)
    - User Guide
    ```
  </action>
  <verify>
    <automated>test -f docs/userguide.md && test -f docs/CHANGELOG.md && grep -q "1.0.0" docs/CHANGELOG.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/userguide.md contains sections for Topology Diagram, Rack Elevation, and Exporting Results (new Phase 4 features)
    - docs/userguide.md mentions all 5 Dell switch models by name
    - docs/userguide.md describes oversubscription color coding (green, amber, red)
    - docs/CHANGELOG.md exists and contains `[1.0.0]` version entry
    - docs/CHANGELOG.md lists sizing engine, hardware catalog, topology diagram, CSV export, PDF export, i18n among features
    - docs/CHANGELOG.md follows keepachangelog.com format with `### Added` section
  </acceptance_criteria>
  <done>User Guide updated with Phase 4 features. Changelog records v1.0.0 with complete feature list.</done>
</task>

</tasks>

<verification>
- 4 new ADR files exist in docs/adr/ (0005-0008)
- docs/prd.md contains all 28 requirement IDs
- docs/userguide.md has Topology and Export sections
- docs/CHANGELOG.md has v1.0.0 entry
</verification>

<success_criteria>
4 new ADRs, updated PRD and User Guide, and new Changelog complete the v1.0 milestone documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visualization-export-and-documentation/04-05-SUMMARY.md`
</output>
